# v0.2.9 — Agent 架构重构实施计划

> 计划生成时间：2026-04-12
> 目标：将 SpiderAgent/DreamAgent 重构为真正的 Agent（Nanobot 框架），实现 18 个 Tool，Neo4j KG 存储，Self-Evolution 引擎

---

## TL;DR

> **Quick Summary**: 大规模 Agent 架构重构，将现有 SpiderAgent/DreamAgent 迁移到统一的 CAAgent 框架，实现 Nanobot ReAct 循环、18 个 Tool、Neo4j KG 存储、Self-Evolution 自进化引擎。
>
> **Deliverables**:
> - CAAgent 统一 Agent 类 + ExploreAgent/DreamAgent 配置实例
> - 18 个 Tool（KG 9 + Queue 5 + Search 5 + LLM 2）
> - Neo4j KG 存储层 + 数据迁移脚本
> - Self-Evolution 引擎 + Daemon 进程
> - 90%+ 测试覆盖率
>
> **Estimated Effort**: XL（大型重构，约 40+ 任务）
> **Parallel Execution**: YES - 6 waves
> **Critical Path**: Wave 1 → Wave 2 → Wave 3 → Wave 4 → Wave 5 → Wave 6 → FINAL

---

## Context

### Original Request
将 v0.2.9 版本计划转化为可执行的开发计划，一次性完整实现。

### Interview Summary
**Key Discussions**:
- 实施策略：一次性完整实现（不分阶段）
- KG 存储：Neo4j + JSON 迁移
- 测试策略：TDD（RED-GREEN-REFACTOR）
- API 兼容：允许变化（追求最佳架构）
- LLM Provider：多 Provider 支持

**Research Findings**:
- Hermes 项目可访问：error_classifier.py（813 行）、retry_utils.py
- Nanobot 项目可访问：agent/loop.py（392 行）、heartbeat/service.py（130 行）
- 现有代码量：SpiderAgent 14275 行、DreamAgent 17402 行、knowledge_graph.py 58667 行

### Guardrails Applied (from Metis/self-review)
- **不删除现有代码**：迁移后保留兼容层
- **保持 config.json 结构**：只扩展，不破坏现有配置
- **不修改 R1D3 接口**：R1D3ToolHandler 保持不变
- **外部模块复制后不跟踪上游**：手动同步策略

---

## Work Objectives

### Core Objective
重构 Agent 架构，实现统一 CAAgent 框架、18 个 Tool、Neo4j KG、Self-Evolution 引擎。

### Concrete Deliverables
- `core/frameworks/error_classifier.py` - Hermes 错误分类
- `core/frameworks/retry_utils.py` - Hermes 重试策略
- `core/frameworks/heartbeat.py` - Nanobot HeartbeatService
- `core/frameworks/agent_runner.py` - Nanobot AgentLoop（修改）
- `core/frameworks/agent_hook.py` - Nanobot Hook 系统（修改）
- `core/tools/base.py` - Tool 基类
- `core/tools/registry.py` - ToolRegistry
- `core/tools/kg_tools.py` - 9 个 KG Tool
- `core/tools/queue_tools.py` - 5 个 Queue Tool
- `core/tools/search_tools.py` - 5 个 Search Tool
- `core/tools/llm_tools.py` - 2 个 LLM Tool
- `core/kg/neo4j_client.py` - Neo4j 操作封装
- `core/kg/kg_repository.py` - KG Repository 抽象
- `core/agents/ca_agent.py` - 统一 Agent 类
- `core/agents/explore_agent.py` - ExploreAgent 配置实例
- `core/agents/dream_agent.py` - DreamAgent 多周期架构
- `core/agents/hooks/*.py` - Agent Hooks
- `core/agents/evolution.py` - Self-Evolution 引擎
- `core/daemon/explore_daemon.py` - ExploreAgent Daemon
- `core/daemon/dream_daemon.py` - DreamAgent Daemon
- `migrations/migrate_json_to_neo4j.py` - 数据迁移脚本
- 测试文件（覆盖率 90%+）

### Definition of Done
- [ ] 所有 18 个 Tool 实现并通过测试
- [ ] CAAgent + ExploreAgent + DreamAgent 通过集成测试
- [ ] Neo4j KG 正常工作，数据迁移完成
- [ ] Self-Evolution 正常记录和更新策略权重
- [ ] 测试覆盖率 ≥ 90%
- [ ] E2E 测试：完整探索流程通过

### Must Have
- Nanobot AgentLoop + Hermes ErrorClassifier + HeartbeatService
- 18 个 Tool 全部实现
- ExploreAgent ReAct 循环正常
- DreamAgent 多周期循环正常
- Neo4j KG 存储正常
- TDD 测试覆盖率 90%+

### Must NOT Have (Guardrails)
- 删除现有 SpiderAgent/DreamAgent/knowledge_graph.py（保留兼容层）
- 破坏 config.json 现有结构
- 修改 R1D3ToolHandler 接口
- 引入外部框架依赖（复制后独立维护）

---

## Verification Strategy

### Test Decision
- **Infrastructure exists**: YES（pytest + tests/）
- **Automated tests**: TDD（RED-GREEN-REFACTOR）
- **Framework**: pytest
- **TDD**: 每个任务先写测试（RED），再实现（GREEN），再重构（REFACTOR）

### QA Policy
每个任务包含 Agent-Executed QA Scenarios：
- Evidence saved to `.sisyphus/evidence/task-{N}-{scenario-slug}.{ext}`
- Frontend/UI: Playwright（本项目主要是 Python Agent，不需要）
- CLI: Bash（运行命令，检查输出）
- API: curl（测试 API 端点）
- Module: pytest（运行单元测试）

---

## Execution Strategy

### Parallel Execution Waves

```
Wave 1 (Start Immediately - 框架基础):
├── T1: Copy error_classifier.py from Hermes [quick]
├── T2: Copy retry_utils.py from Hermes [quick]
├── T3: Copy heartbeat.py from Nanobot [quick]
├── T4: Copy and modify agent_runner.py [unspecified-high]
├── T5: Copy and modify agent_hook.py [unspecified-high]
└── T6: Create tool_registry.py [unspecified-high]

Wave 2 (After Wave 1 - Tool 基类 + Neo4j):
├── T7: Create Tool base class [quick]
├── T8: Configure Neo4j connection [unspecified-high]
├── T9: Create neo4j_client.py [deep]
└── T10: Create kg_repository.py [deep]

Wave 3 (After Wave 2 - Tool 实现):
├── T11: Implement KG Tools (9 tools) [unspecified-high]
├── T12: Implement Queue Tools (5 tools) [unspecified-high]
├── T13: Implement Search Tools (5 tools) [unspecified-high]
└── T14: Implement LLM Tools (2 tools) [unspecified-high]

Wave 4 (After Wave 3 - Agent 实现):
├── T15: Create CAAgent unified class [deep]
├── T16: Implement ExploreAgent (ReAct) [deep]
├── T17: Implement DreamAgent (multi-cycle) [ultrabrain]
├── T18: Implement Agent Hooks [unspecified-high]
└── T19: Implement Self-Evolution engine [deep]

Wave 5 (After Wave 4 - Daemon + Config):
├── T20: Implement explore_daemon.py [unspecified-high]
├── T21: Implement dream_daemon.py [unspecified-high]
├── T22: Create agent configurations [quick]
└── T23: Create LLM providers config [quick]

Wave 6 (After Wave 5 - 数据迁移):
├── T24: Create migration script [deep]
├── T25: Test migration with sample data [unspecified-high]
└── T26: Update CLI/API to use new agents [unspecified-high]

Wave FINAL (After Wave 6 — 验证):
├── F1: Plan compliance audit (oracle)
├── F2: Code quality review (unspecified-high)
├── F3: Integration test run (unspecified-high)
└── F4: E2E exploration test (deep)
```

Critical Path: T1-T6 → T7-T10 → T11-T14 → T15-T19 → T20-T23 → T24-T26 → F1-F4

---

## TODOs

- [x] 1. **Copy error_classifier.py from Hermes**

  **What to do**:
  - Copy `/root/dev/hermes-agent/agent/error_classifier.py` to `core/frameworks/error_classifier.py`
  - Review imports and adjust for CA project structure
  - Add unit tests: `tests/core/frameworks/test_error_classifier.py`
  - Test: FailoverReason enum, ClassifiedError dataclass, classify() method

  **Must NOT do**:
  - Do not add Hermes dependencies (keep standalone)
  - Do not modify the core logic (copy first, adapt later if needed)

  **Recommended Agent Profile**:
  - **Category**: `quick` (simple file copy + test)
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T2, T3)
  - **Parallel Group**: Wave 1
  - **Blocks**: T4 (agent_runner needs error_classifier)

  **References**:
  - Source: `/root/dev/hermes-agent/agent/error_classifier.py:1-813` - Full file to copy
  - Test pattern: `tests/core/test_knowledge_graph.py` - Reference for test structure

  **Acceptance Criteria**:
  - [ ] File exists at `core/frameworks/error_classifier.py`
  - [ ] `pytest tests/core/frameworks/test_error_classifier.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: Error classification works
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/frameworks/test_error_classifier.py -v
    Expected: All tests pass (enum, dataclass, classify)
    Evidence: .sisyphus/evidence/task-1-error-classifier.log
  ```

  **Commit**: YES (groups with Wave 1)
  - Message: `refactor(frameworks): copy error_classifier from Hermes`
  - Files: `core/frameworks/error_classifier.py`, `tests/core/frameworks/test_error_classifier.py`

- [x] 2. **Copy retry_utils.py from Hermes**

  **What to do**:
  - Copy `/root/dev/hermes-agent/agent/retry_utils.py` to `core/frameworks/retry_utils.py`
  - Review imports and adjust for CA project structure
  - Add unit tests: `tests/core/frameworks/test_retry_utils.py`
  - Test: retry_with_backoff, exponential backoff, max retries

  **Must NOT do**:
  - Do not add Hermes dependencies
  - Do not modify core logic

  **Recommended Agent Profile**:
  - **Category**: `quick`
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T1, T3)
  - **Parallel Group**: Wave 1
  - **Blocks**: T4

  **References**:
  - Source: `/root/dev/hermes-agent/agent/retry_utils.py` - Full file to copy

  **Acceptance Criteria**:
  - [ ] File exists at `core/frameworks/retry_utils.py`
  - [ ] `pytest tests/core/frameworks/test_retry_utils.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: Retry with exponential backoff
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/frameworks/test_retry_utils.py -v
    Expected: All tests pass
    Evidence: .sisyphus/evidence/task-2-retry-utils.log
  ```

  **Commit**: YES (groups with Wave 1)

- [x] 3. **Copy heartbeat.py from Nanobot**

  **What to do**:
  - Copy `/root/dev/nanobot/nanobot/heartbeat/service.py` to `core/frameworks/heartbeat.py`
  - Rename HeartbeatService to CAHeartbeatService (optional, for clarity)
  - Remove Nanobot-specific dependencies (bus, workspace)
  - Add unit tests: `tests/core/frameworks/test_heartbeat.py`
  - Test: start(), stop(), trigger_now(), interval configuration

  **Must NOT do**:
  - Do not add Nanobot dependencies (MessageBus, SessionManager)
  - Do not keep HEARTBEAT.md file reading (CA uses different trigger)

  **Recommended Agent Profile**:
  - **Category**: `quick`
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T1, T2)
  - **Parallel Group**: Wave 1
  - **Blocks**: T21 (dream_daemon uses heartbeat)

  **References**:
  - Source: `/root/dev/nanobot/nanobot/heartbeat/service.py:1-130` - HeartbeatService to copy
  - Adapt pattern: Keep async start/stop, remove HEARTBEAT.md logic

  **Acceptance Criteria**:
  - [ ] File exists at `core/frameworks/heartbeat.py`
  - [ ] `pytest tests/core/frameworks/test_heartbeat.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: Heartbeat interval configurable
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/frameworks/test_heartbeat.py -v
    Expected: All tests pass (start, stop, trigger)
    Evidence: .sisyphus/evidence/task-3-heartbeat.log
  ```

  **Commit**: YES (groups with Wave 1)

- [x] 4. **Copy and modify agent_runner.py**

  **What to do**:
  - Copy `/root/dev/nanobot/nanobot/agent/loop.py` to `core/frameworks/agent_runner.py`
  - Rename AgentLoop to CAAgentRunner
  - Remove Nanobot-specific: MessageBus, SessionManager, SubagentManager
  - Keep: ReAct loop, ToolRegistry, LLM Provider, max_iterations
  - Add integration with Hermes error_classifier for retry logic
  - Add unit tests: `tests/core/frameworks/test_agent_runner.py`

  **Must NOT do**:
  - Do not keep Nanobot MessageBus (CA uses Queue for input)
  - Do not keep SessionManager (CA has no cross-session memory)
  - Do not keep SubagentManager (CA not needed for now)

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high` (moderate complexity, requires understanding Nanobot)
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: NO (needs T1, T2 for error_classifier/retry_utils)
  - **Parallel Group**: Wave 1 (sequential after T1-T3)
  - **Blocks**: T15 (CAAgent uses agent_runner)

  **References**:
  - Source: `/root/dev/nanobot/nanobot/agent/loop.py:1-100` - AgentLoop core logic
  - Integration: `core/frameworks/error_classifier.py` - Use for error handling
  - Integration: `core/frameworks/retry_utils.py` - Use for retry

  **Acceptance Criteria**:
  - [ ] File exists at `core/frameworks/agent_runner.py`
  - [ ] ReAct loop implemented (thought → action → observation)
  - [ ] Integration with error_classifier working
  - [ ] `pytest tests/core/frameworks/test_agent_runner.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: ReAct loop executes tools
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/frameworks/test_agent_runner.py -v
    Expected: ReAct loop tests pass
    Evidence: .sisyphus/evidence/task-4-agent-runner.log
  ```

  **Commit**: YES (groups with Wave 1)

- [x] 5. **Copy and modify agent_hook.py**

  **What to do**:
  - Create `core/frameworks/agent_hook.py` with AgentHook base class
  - Define hooks: before_iteration, after_iteration, on_tool_call, on_error, on_complete
  - Reference Nanobot's hook pattern but simplify for CA
  - Add unit tests: `tests/core/frameworks/test_agent_hook.py`

  **Must NOT do**:
  - Do not copy Nanobot's full hook system (too complex)
  - Do not add before_phase/after_phase (DreamAgent-specific, will be in dream_hook)

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T4)
  - **Parallel Group**: Wave 1
  - **Blocks**: T18 (Agent Hooks implementation)

  **References**:
  - Pattern: Nanobot hook concept (lightweight base class)

  **Acceptance Criteria**:
  - [ ] File exists at `core/frameworks/agent_hook.py`
  - [ ] AgentHook base class with 5 hook methods defined
  - [ ] `pytest tests/core/frameworks/test_agent_hook.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: Hooks are called in order
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/frameworks/test_agent_hook.py -v
    Expected: Hook call order tests pass
    Evidence: .sisyphus/evidence/task-5-agent-hook.log
  ```

  **Commit**: YES (groups with Wave 1)

- [x] 6. **Create tool_registry.py**

  **What to do**:
  - Create `core/tools/registry.py` with ToolRegistry class
  - Implement register(tool), get(tool_name), list_tools()
  - Support tool discovery for LLM (JSON Schema generation)
  - Add unit tests: `tests/core/tools/test_registry.py`

  **Must NOT do**:
  - Do not copy Nanobot's full registry (too many built-in tools)
  - Do not pre-register any tools (CA will register its own)

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T4, T5)
  - **Parallel Group**: Wave 1
  - **Blocks**: T11-T14 (Tool implementations need registry)

  **References**:
  - Pattern: `/root/dev/nanobot/nanobot/agent/tools/registry.py` - Reference pattern

  **Acceptance Criteria**:
  - [ ] File exists at `core/tools/registry.py`
  - [ ] ToolRegistry with register/get/list working
  - [ ] JSON Schema generation for tool calling
  - [ ] `pytest tests/core/tools/test_registry.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: Tool registry can register and retrieve tools
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/tools/test_registry.py -v
    Expected: Registry tests pass
    Evidence: .sisyphus/evidence/task-6-tool-registry.log
  ```

  **Commit**: YES (Wave 1 complete)
  - Message: `refactor(frameworks): copy Hermes and Nanobot modules`
  - Files: All Wave 1 files

- [x] 7. **Create Tool base class**

  **What to do**:
  - Create `core/tools/base.py` with BaseTool class
  - Define: name, description, forward(**kwargs), input_schema
  - Add validation: input schema validation before forward()
  - Add unit tests: `tests/core/tools/test_base.py`

  **Must NOT do**:
  - Do not add Nanobot-specific tool patterns (filesystem, shell, etc.)

  **Recommended Agent Profile**:
  - **Category**: `quick`
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T8, T9, T10 after Wave 1)
  - **Parallel Group**: Wave 2
  - **Blocks**: T11-T14 (all tools inherit from BaseTool)

  **References**:
  - Pattern: BaseTool concept from Nanobot (simplified)

  **Acceptance Criteria**:
  - [ ] File exists at `core/tools/base.py`
  - [ ] BaseTool with name, description, forward, input_schema
  - [ ] Input validation working
  - [ ] `pytest tests/core/tools/test_base.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: BaseTool validates inputs
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/tools/test_base.py -v
    Expected: Validation tests pass
    Evidence: .sisyphus/evidence/task-7-tool-base.log
  ```

  **Commit**: YES (groups with Wave 2)

- [x] 8. **Install and Configure Neo4j**

  **What to do**:
  - Install Neo4j (Docker recommended: `docker run -d -p 7474:7474 -p 7687:7687 neo4j`)
  - Configure: set password, create database
  - Add Neo4j config to `config.json` or `.env` (URI, username, password)
  - Test connection with Neo4j driver
  - Add tests: `tests/core/kg/test_neo4j_install.py`

  **Must NOT do**:
  - Do not hardcode credentials in code
  - Do not expose Neo4j port publicly (use internal network)

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high` (requires infrastructure setup)
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: NO (must complete before T9)
  - **Parallel Group**: Wave 2 (sequential first)
  - **Blocks**: T9, T10

  **References**:
  - Docker: `docker run neo4j` documentation
  - Python driver: `neo4j` package documentation

  **Acceptance Criteria**:
  - [ ] Neo4j running (docker ps shows neo4j container)
  - [ ] Connection test successful
  - [ ] Config stored securely
  - [ ] `pytest tests/core/kg/test_neo4j_install.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: Neo4j is running and accessible
    Tool: Bash (docker + pytest)
    Steps:
      1. docker ps | grep neo4j
      2. pytest tests/core/kg/test_neo4j_install.py -v
    Expected: Neo4j container running, connection test passes
    Evidence: .sisyphus/evidence/task-8-neo4j-install.log
  ```

  **Commit**: YES (groups with Wave 2)

- [x] 9. **Create neo4j_client.py**

  **What to do**:
  - Create `core/kg/neo4j_client.py` with Neo4jClient class
  - Implement: connect(), disconnect(), execute_query(), create_node(), query_node()
  - Add connection pooling and error handling
  - Add unit tests: `tests/core/kg/test_neo4j_client.py`
  - Mock Neo4j for tests (use neo4j-mock or in-memory)

  **Must NOT do**:
  - Do not expose raw Neo4j driver (wrap in client)
  - Do not add KG-specific logic (that goes in kg_repository)

  **Recommended Agent Profile**:
  - **Category**: `deep` (core infrastructure)
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T7, T8, T10 after Wave 1)
  - **Parallel Group**: Wave 2
  - **Blocks**: T10, T11 (kg_repository and KG Tools need client)

  **References**:
  - Neo4j Python driver: `neo4j` package documentation
  - Pattern: Repository pattern for database abstraction

  **Acceptance Criteria**:
  - [ ] File exists at `core/kg/neo4j_client.py`
  - [ ] connect/disconnect/execute_query working
  - [ ] Error handling with retry (use retry_utils)
  - [ ] `pytest tests/core/kg/test_neo4j_client.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: Neo4j client executes queries
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/kg/test_neo4j_client.py -v
    Expected: Query tests pass (mock or real)
    Evidence: .sisyphus/evidence/task-9-neo4j-client.log
  ```

  **Commit**: YES (groups with Wave 2)

- [x] 10. **Create kg_repository.py**

  **What to do**:
  - Create `core/kg/kg_repository.py` with KGRepository class
  - Implement KG-specific operations:
    - create_knowledge_node(topic, content, source_urls, relations)
    - query_knowledge(topic, limit)
    - update_status(topic, status)
    - update_metadata(topic, heat, quality, confidence)
    - get_relations(topic)
    - merge_nodes(node_ids)
  - Add unit tests: `tests/core/kg/test_kg_repository.py`
  - Use neo4j_client for database operations

  **Must NOT do**:
  - Do not add business logic (belongs to Agent)
  - Do not expose Cypher queries directly (wrap in methods)

  **Recommended Agent Profile**:
  - **Category**: `deep`
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T7, T8, T9)
  - **Parallel Group**: Wave 2
  - **Blocks**: T11 (KG Tools use kg_repository)

  **References**:
  - Source: `core/knowledge_graph.py:1-100` - Current KG logic to reference
  - Schema: Knowledge node data model from design doc

  **Acceptance Criteria**:
  - [ ] File exists at `core/kg/kg_repository.py`
  - [ ] All KG operations implemented
  - [ ] `pytest tests/core/kg/test_kg_repository.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: KGRepository creates and queries nodes
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/kg/test_kg_repository.py -v
    Expected: Node operations tests pass
    Evidence: .sisyphus/evidence/task-10-kg-repository.log
  ```

  **Commit**: YES (Wave 2 complete)
  - Message: `feat(kg): add Neo4j client and repository`
  - Files: All Wave 2 files

- [x] 11. **Implement KG Tools (9 tools)**

  **What to do**:
  - Create `core/tools/kg_tools.py` with 9 KG Tools
  - Tools: query_kg, query_kg_by_status, query_kg_by_heat, get_node_relations, add_to_kg, update_kg_status, update_kg_metadata, update_kg_relation, merge_kg_nodes
  - Each tool inherits from BaseTool
  - Each tool has: name, description, forward(), input_schema
  - forward() calls kg_repository methods
  - Add tests: `tests/core/tools/test_kg_tools.py`

  **Must NOT do**:
  - Do not bypass kg_repository (always use repository)
  - Do not add tools that write KG in DreamAgent's toolset

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high` (9 tools, moderate effort)
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T12, T13, T14 after Wave 2)
  - **Parallel Group**: Wave 3
  - **Blocks**: T15 (CAAgent needs tools)

  **References**:
  - Repository: `core/kg/kg_repository.py` - Methods to call
  - Base: `core/tools/base.py` - BaseTool to inherit

  **Acceptance Criteria**:
  - [ ] File exists at `core/tools/kg_tools.py`
  - [ ] All 9 tools implemented
  - [ ] Each tool registered in ToolRegistry
  - [ ] `pytest tests/core/tools/test_kg_tools.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: KG tools can query and modify nodes
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/tools/test_kg_tools.py -v
    Expected: All 9 tool tests pass
    Evidence: .sisyphus/evidence/task-11-kg-tools.log
  ```

  **Commit**: YES (groups with Wave 3)

- [x] 12. **Implement Queue Tools (5 tools)**

  **What to do**:
  - Create `core/tools/queue_tools.py` with 5 Queue Tools
  - Tools: add_to_queue, claim_queue, get_queue, mark_done, mark_failed
  - Use SQLite for queue storage (create queue.db)
  - Atomic claim with holder_id and timeout (claim_queue)
  - Add tests: `tests/core/tools/test_queue_tools.py`

  **Must NOT do**:
  - Do not use file-based queue (use SQLite)
  - Do not add blocking operations (async where needed)

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T11, T13, T14)
  - **Parallel Group**: Wave 3
  - **Blocks**: T15, T16 (Agents use queue tools)

  **References**:
  - Current: `core/queue_service.py` - Existing queue logic
  - Pattern: Atomic claim with timeout

  **Acceptance Criteria**:
  - [ ] File exists at `core/tools/queue_tools.py`
  - [ ] All 5 tools implemented
  - [ ] SQLite queue.db created
  - [ ] Atomic claim working (timeout release)
  - [ ] `pytest tests/core/tools/test_queue_tools.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: Queue tools handle atomic claim
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/tools/test_queue_tools.py -v
    Expected: Claim and timeout tests pass
    Evidence: .sisyphus/evidence/task-12-queue-tools.log
  ```

  **Commit**: YES (groups with Wave 3)

- [x] 13. **Implement Search Tools (5 tools)**

  **What to do**:
  - Create `core/tools/search_tools.py` with 5 Search Tools
  - Tools: search_web, fetch_page, download_paper, parse_pdf, process_paper
  - Multi-provider support: Bocha, Serper, Google (configurable)
  - PDF handling: use PyPDF2 or pdfplumber
  - Add tests: `tests/core/tools/test_search_tools.py`
  - Mock external APIs for tests

  **Must NOT do**:
  - Do not hardcode API keys (use config/.env)
  - Do not add blocking HTTP calls (use async aiohttp)

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T11, T12, T14)
  - **Parallel Group**: Wave 3
  - **Blocks**: T16 (ExploreAgent uses search tools)

  **References**:
  - Current: `core/providers/bocha_provider.py`, `core/providers/serper_provider.py`
  - HTTP: aiohttp for async fetching

  **Acceptance Criteria**:
  - [ ] File exists at `core/tools/search_tools.py`
  - [ ] All 5 tools implemented
  - [ ] Multi-provider support working
  - [ ] PDF parsing working
  - [ ] `pytest tests/core/tools/test_search_tools.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: Search tools fetch web content
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/tools/test_search_tools.py -v
    Expected: Search and fetch tests pass (mocked)
    Evidence: .sisyphus/evidence/task-13-search-tools.log
  ```

  **Commit**: YES (groups with Wave 3)

- [x] 14. **Implement LLM Tools (2 tools)**

  **What to do**:
  - Create `core/tools/llm_tools.py` with 2 LLM Tools
  - Tools: llm_analyze, llm_summarize
  - Multi-provider support: volcengine, minimax (configurable priority)
  - Use existing LLM client infrastructure
  - Add tests: `tests/core/tools/test_llm_tools.py`
  - Mock LLM calls for tests

  **Must NOT do**:
  - Do not hardcode provider selection (use config)
  - Do not add complex LLM orchestration (keep simple forward())

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T11, T12, T13)
  - **Parallel Group**: Wave 3
  - **Blocks**: T15, T17 (Agents use LLM tools)

  **References**:
  - Current: `core/llm_client.py` - Existing LLM infrastructure
  - Providers: volcengine API, minimax API

  **Acceptance Criteria**:
  - [ ] File exists at `core/tools/llm_tools.py`
  - [ ] Both tools implemented
  - [ ] Multi-provider working
  - [ ] `pytest tests/core/tools/test_llm_tools.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: LLM tools analyze and summarize
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/tools/test_llm_tools.py -v
    Expected: LLM tool tests pass (mocked)
    Evidence: .sisyphus/evidence/task-14-llm-tools.log
  ```

  **Commit**: YES (Wave 3 complete)
  - Message: `feat(tools): implement all 18 tools`
  - Files: All Wave 3 files

- [x] 15. **Create CAAgent unified class**

  **What to do**:
  - Create `core/agents/ca_agent.py` with CAAgent class
  - Unified Agent that ExploreAgent/DreamAgent inherit/configure
  - Core components: AgentRunner, ToolRegistry, ErrorClassifier, AgentHook
  - Config: CAAgentConfig (name, system_prompt, tools, max_iterations, model)
  - Method: run(input_data) → AgentResult
  - Add tests: `tests/core/agents/test_ca_agent.py`

  **Must NOT do**:
  - Do not add ReAct loop in base class (DreamAgent doesn't use it)
  - Do not add KG/Queue business logic in base class

  **Recommended Agent Profile**:
  - **Category**: `deep` (core architecture)
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: NO (needs Wave 1-3 complete)
  - **Parallel Group**: Wave 4
  - **Blocks**: T16, T17

  **References**:
  - Frameworks: `core/frameworks/agent_runner.py`, `core/frameworks/agent_hook.py`
  - Tools: `core/tools/registry.py`

  **Acceptance Criteria**:
  - [ ] File exists at `core/agents/ca_agent.py`
  - [ ] CAAgentConfig dataclass defined
  - [ ] CAAgent with run() method working
  - [ ] `pytest tests/core/agents/test_ca_agent.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: CAAgent runs with configured tools
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/agents/test_ca_agent.py -v
    Expected: CAAgent tests pass
    Evidence: .sisyphus/evidence/task-15-ca-agent.log
  ```

  **Commit**: YES (groups with Wave 4)

- [x] 16. **Implement ExploreAgent (ReAct)**

  **What to do**:
  - Create `core/agents/explore_agent.py` with ExploreAgent class
  - Extends CAAgent with Explore-specific config
  - ReAct loop: Claim topic → Thought→Action→Observation (max 10 iterations) → Write KG → Mark done
  - System prompt: exploration-focused (from design doc)
  - Tool subset: 14 tools (KG write + Queue + Search + LLM)
  - Add tests: `tests/core/agents/test_explore_agent.py`

  **Must NOT do**:
  - Do not skip ReAct loop
  - Do not add DreamAgent's multi-cycle logic

  **Recommended Agent Profile**:
  - **Category**: `deep`
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T17)
  - **Parallel Group**: Wave 4
  - **Blocks**: T20

  **References**:
  - Base: `core/agents/ca_agent.py`
  - Prompt: Design doc Section 2.1
  - Tools: 14 tools from kg/queue/search/llm

  **Acceptance Criteria**:
  - [ ] File exists at `core/agents/explore_agent.py`
  - [ ] ReAct loop implemented
  - [ ] Explores topic from queue, writes to KG
  - [ ] `pytest tests/core/agents/test_explore_agent.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: ExploreAgent explores a topic via ReAct
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/agents/test_explore_agent.py -v
    Expected: ReAct loop tests pass
    Evidence: .sisyphus/evidence/task-16-explore-agent.log
  ```

  **Commit**: YES (groups with Wave 4)

- [x] 17. **Implement DreamAgent (multi-cycle)**

  **What to do**:
  - Create `core/agents/dream_agent.py` with DreamAgent class
  - Multi-cycle architecture: 3-4 Mini-Cycles, each L1→L2→L3→L4
  - L1 Light Sleep: Candidate selection (ExplorationLog + KG anomalies)
  - L2 Deep Sleep: 6-dimension scoring (Relevance, Frequency, etc.)
  - L3 Filtering: Threshold gating (minScore≥0.8, minRecallCount≥3)
  - L4 REM Sleep: Queue topic generation (no KG write)
  - Tool subset: 15 tools (KG query + Memory + Queue + LLM, NO KG write)
  - Add tests: `tests/core/agents/test_dream_agent.py`

  **Must NOT do**:
  - Do NOT write KG (DreamAgent only generates Queue topics)
  - Do not use ReAct loop (DreamAgent uses linear pipeline)

  **Recommended Agent Profile**:
  - **Category**: `ultrabrain` (complex multi-cycle logic)
  - **Skills**: none

  **Parallelization**:
  - **Can Run In Parallel**: YES (with T16)
  - **Parallel Group**: Wave 4
  - **Blocks**: T21

  **References**:
  - Design doc Section 6.2-6.3 - Multi-cycle architecture
  - Scoring: 6-dimension weights from design doc
  - Tools: KG query (not write), Queue write, LLM

  **Acceptance Criteria**:
  - [ ] File exists at `core/agents/dream_agent.py`
  - [ ] 4 layers implemented (Light/Deep/Filtering/REM)
  - [ ] 6-dimension scoring working
  - [ ] Threshold gating working
  - [ ] Only Queue output (no KG write)
  - [ ] `pytest tests/core/agents/test_dream_agent.py` → PASS

  **QA Scenarios**:
  ```
  Scenario: DreamAgent runs multi-cycle and generates Queue topics
    Tool: Bash (pytest)
    Steps:
      1. pytest tests/core/agents/test_dream_agent.py -v
    Expected: Multi-cycle tests pass
    Evidence: .sisyphus/evidence/task-17-dream-agent.log
  ```

  **Commit**: YES (groups with Wave 4)

- [x] 18. **Implement Agent Hooks**

  **What to do**:
  - Create `core/agents/hooks/` directory with base.py, explore_hook.py, dream_hook.py
  - ExploreHook: log exploration, quality check
  - DreamHook: phase logging, Queue persistence
  - Add tests: `tests/core/agents/test_hooks.py`

  **Recommended Agent Profile**: `unspecified-high`

  **Parallelization**: YES (with T16, T17) | Wave 4

  **Acceptance Criteria**:
  - [ ] Files exist in `core/agents/hooks/`
  - [ ] Tests pass

  **Commit**: YES (groups with Wave 4)

- [x] 19. **Implement Self-Evolution engine**

  **What to do**:
  - Create `core/agents/evolution.py` with ExplorationEvolution class
  - Record ExplorationLog, Bayesian weight update
  - Strategy weights stored in `core/kg/strategy_weights.py`
  - Add tests: `tests/core/agents/test_evolution.py`

  **Recommended Agent Profile**: `deep`

  **Parallelization**: YES (with T16-T18) | Wave 4

  **Acceptance Criteria**:
  - [ ] File exists
  - [ ] Evolution tests pass

  **Commit**: YES (Wave 4 complete)

- [x] 20. **Implement explore_daemon.py**

  **What to do**:
  - Create `core/daemon/explore_daemon.py` with ExploreDaemon
  - Continuous loop: claim → explore → mark done
  - Graceful shutdown
  - Add tests: `tests/core/daemon/test_explore_daemon.py`

  **Recommended Agent Profile**: `unspecified-high`

  **Parallelization**: YES (with T21-T23) | Wave 5

  **Acceptance Criteria**:
  - [ ] File exists, daemon tests pass

  **Commit**: YES (Wave 5)

- [x] 21. **Implement dream_daemon.py**

  **What to do**:
  - Create `core/daemon/dream_daemon.py` with DreamDaemon
  - Heartbeat trigger (6h interval), use HeartbeatService
  - Add tests: `tests/core/daemon/test_dream_daemon.py`

  **Recommended Agent Profile**: `unspecified-high`

  **Parallelization**: YES (with T20, T22, T23) | Wave 5

  **Acceptance Criteria**:
  - [ ] File exists, heartbeat tests pass

  **Commit**: YES (Wave 5)

- [x] 22. **Create agent configurations**

  **What to do**:
  - Create `core/configs/agent_explore.py` - ExploreAgent config (system prompt, 14 tools)
  - Create `core/configs/agent_dream.py` - DreamAgent config (multi-cycle params, 15 tools)
  - Add tests: `tests/core/configs/test_configs.py`

  **Recommended Agent Profile**: `quick`

  **Parallelization**: YES (Wave 5)

  **Acceptance Criteria**:
  - [ ] Config files exist, tests pass

  **Commit**: YES (Wave 5)

- [x] 23. **Create LLM providers config**

  **What to do**:
  - Create `core/configs/llm_providers.py` - Multi-provider config (volcengine, minimax)
  - Provider priority, API key management
  - Add tests

  **Recommended Agent Profile**: `quick`

  **Parallelization**: YES (Wave 5)

  **Acceptance Criteria**:
  - [ ] Multi-provider config exists

  **Commit**: YES (Wave 5 complete)

- [x] 24. **Create migration script**

  **What to do**:
  - Create `migrations/migrate_json_to_neo4j.py`
  - Read existing state.json/knowledge_graph.db
  - Convert to Neo4j nodes
  - Validate migration (node count match)
  - Add tests: `tests/migrations/test_migrate.py`

  **Recommended Agent Profile**: `deep`

  **Parallelization**: YES (with T25, T26) | Wave 6

  **Acceptance Criteria**:
  - [ ] Migration script exists, validates correctly

  **Commit**: YES (Wave 6)

- [x] 25. **Test migration with sample data**

  **What to do**:
  - Run migration on sample data
  - Verify Neo4j node count matches JSON count
  - Verify node properties preserved
  - Add rollback mechanism

  **Recommended Agent Profile**: `unspecified-high`

  **Parallelization**: YES (Wave 6)

  **Acceptance Criteria**:
  - [ ] Migration succeeds, validation passes

  **Commit**: YES (Wave 6)

- [x] 26. **Update CLI/API to use new agents**

  **What to do**:
  - Update `curious_agent.py` CLI to use new CAAgent framework
  - Update `curious_api.py` API endpoints to use new ExploreAgent/DreamAgent
  - Add compatibility layer for old calls (optional)
  - Add tests: integration tests for CLI/API

  **Recommended Agent Profile**: `unspecified-high`

  **Parallelization**: YES (Wave 6)

  **Acceptance Criteria**:
  - [ ] CLI/API working with new agents

  **Commit**: YES (Wave 6 complete)

---

## Final Verification Wave

- [x] F1. **Plan Compliance Audit** — oracle
  - 验证所有 Must Have 已实现
  - 验证所有 Must NOT Have 未出现
  - 检查 evidence 文件存在

- [x] F2. **Code Quality Review** — unspecified-high
  - Run `pytest` + `ruff` (or flake8)
  - 检查 `as any`、`@ts-ignore`（Python 对应：`# type: ignore`）
  - 检查 unused imports
  - 检查 AI slop: excessive comments, over-abstraction

- [x] F3. **Integration Test Run** — unspecified-high
  - Agent + Tool + KG 联动测试
  - ExploreAgent 探索完整流程
  - DreamAgent 多周期完整流程

- [x] F4. **E2E Exploration Test** — deep
  - 从 Queue注入 topic
  - ExploreAgent 探索
  - KG 写入验证
  - DreamAgent 生成新 Queue topic
  - Self-Evolution 记录验证

---

## Commit Strategy

按 Wave 提交：
- Wave 1: `refactor(frameworks): copy Hermes and Nanobot modules`
- Wave 2: `feat(kg): add Neo4j client and repository`
- Wave 3: `feat(tools): implement all 18 tools`
- Wave 4: `refactor(agents): implement CAAgent framework`
- Wave 5: `feat(daemon): add agent daemons and configs`
- Wave 6: `feat(migration): add JSON to Neo4j migration`
- FINAL: `test: add comprehensive test suite`

---

## Success Criteria

### Verification Commands
```bash
pytest tests/core/ --cov=core --cov-report=term-missing
# Expected: Coverage ≥ 90%

pytest tests/core/integration/
# Expected: All integration tests pass

python -m core.daemon.explore_daemon --test
# Expected: ExploreAgent runs one topic successfully

python -m core.daemon.dream_daemon --test
# Expected: DreamAgent runs one cycle successfully
```

### Final Checklist
- [x] All 18 Tools implemented and tested
- [x] CAAgent + ExploreAgent + DreamAgent pass integration tests
- [x] Neo4j KG working, migration complete
- [x] Self-Evolution recording and updating weights
- [x] Test coverage ≥ 90%
- [x] E2E test: complete exploration flow passes