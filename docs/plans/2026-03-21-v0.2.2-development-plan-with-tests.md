# Curious Agent v0.2.2 详细开发计划（含100%测试覆盖）

> **目标**: 实现元认知监控器，解决无限循环和价值缺失问题
> **测试要求**: 每个模块100%单元测试覆盖 + 集成测试
> **当前进度**: Step 0-2 已完成，剩余 Step 3-6

---

## 当前状态总结

### 已完成 ✅

| 组件 | 文件 | 状态 |
|------|------|------|
| LLMManager | `core/llm_manager.py` | ✅ 实现 + 测试 |
| LLMClient改造 | `core/llm_client.py` | ✅ 向后兼容 |
| KnowledgeGraph扩展 | `core/knowledge_graph.py` | ✅ 元认知字段 |
| MetaCognitiveMonitor | `core/meta_cognitive_monitor.py` | ✅ 刚完成 |
| Config示例 | `config.json.example` | ✅ 已创建 |

### 待完成 📋

| 步骤 | 组件 | 测试要求 |
|------|------|---------|
| Step 2补全 | MetaCognitiveMonitor测试 | 100%覆盖 |
| Step 3 | MetaCognitiveController | 100%覆盖 |
| Step 4 | MGV循环集成 | 集成测试 |
| Step 5 | EventBus + Config + API | 100%覆盖 |
| Step 6 | Web UI元认知区域 | E2E测试 |
| 验收 | 全链路测试 | 通过验收清单 |

---

## Step 2补全: MetaCognitiveMonitor 测试 (100%覆盖)

### Task 2.1: 创建完整测试套件

**文件:** `tests/test_meta_cognitive_monitor.py`

**测试场景清单:**

```python
# 1. 初始化测试
test_init_without_llm          # 无LLM客户端初始化
test_init_with_llm             # 有LLM客户端初始化

# 2. 只读查询测试 (100%覆盖get_*方法)
test_get_explore_count_new_topic      # 新话题返回0
test_get_explore_count_existing       # 已有话题返回计数
test_get_marginal_returns_empty       # 无历史返回[]
test_get_marginal_returns_with_data   # 有历史返回列表
test_get_last_quality_no_history      # 无历史返回0.0
test_get_last_quality_with_history    # 有历史返回质量分
test_is_topic_blocked_false           # 未阻止返回False
test_is_topic_blocked_true            # 已阻止返回True

# 3. 质量评分算法测试 (核心算法)
test_assess_quality_new_discovery     # 新关键词发现率高
test_assess_quality_depth_improvement # 深度提升
test_assess_quality_user_relevance    # 用户相关性
test_assess_quality_all_dimensions    # 三维综合
test_assess_quality_exception_fallback # 异常时返回5.0

# 4. 边际收益计算测试
test_marginal_return_first_exploration # 首次返回1.0
test_marginal_return_improvement       # 质量提升为正
test_marginal_return_decline           # 质量下降为负
test_marginal_return_stable            # 质量稳定

# 5. 关键词提取测试
test_extract_keywords_with_llm         # LLM可用时使用LLM
test_extract_keywords_fallback         # LLM失败时降级规则
test_extract_keywords_empty_text       # 空文本处理

# 6. 深度评估测试
test_depth_score_long_summary          # 长摘要高分
test_depth_score_many_sources          # 多来源高分
test_depth_score_many_papers           # 多论文高分
test_depth_score_balanced              # 综合评分

# 7. 用户相关性测试
test_user_relevance_with_llm           # LLM评估
-test_user_relevance_keyword_overlap   # 关键词重叠降级
-test_user_relevance_no_interests      # 无兴趣返回0.5

# 8. 记录功能测试
test_record_exploration                # 记录到state.json
```

**实现代码:**

```python
import pytest
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

from unittest.mock import Mock, patch
from core.meta_cognitive_monitor import MetaCognitiveMonitor
from core import knowledge_graph as kg


class TestMetaCognitiveMonitorInit:
    def test_init_without_llm(self):
        monitor = MetaCognitiveMonitor()
        assert monitor.llm is None
        assert monitor.kg is kg

    def test_init_with_llm(self):
        mock_llm = Mock()
        monitor = MetaCognitiveMonitor(llm_client=mock_llm)
        assert monitor.llm is mock_llm


class TestMetaCognitiveMonitorQueries:
    def setup_method(self):
        self.monitor = MetaCognitiveMonitor()
        # Reset state
        state = kg.get_state()
        state["meta_cognitive"] = {
            "explore_counts": {},
            "marginal_returns": {},
            "last_quality": {},
            "exploration_log": [],
            "completed_topics": {}
        }
        kg._save_state(state)

    def test_get_explore_count_new_topic(self):
        assert self.monitor.get_explore_count("new-topic") == 0

    def test_get_explore_count_existing(self):
        kg.update_meta_exploration("test-topic", 7.0, 0.5, False)
        assert self.monitor.get_explore_count("test-topic") == 1

    def test_get_marginal_returns_empty(self):
        assert self.monitor.get_marginal_returns("new-topic") == []

    def test_get_marginal_returns_with_data(self):
        kg.update_meta_exploration("test-topic", 7.0, 0.5, False)
        kg.update_meta_exploration("test-topic", 8.0, 0.3, False)
        returns = self.monitor.get_marginal_returns("test-topic")
        assert len(returns) == 2
        assert returns == [0.5, 0.3]

    def test_get_last_quality_no_history(self):
        assert self.monitor.get_last_quality("new-topic") == 0.0

    def test_get_last_quality_with_history(self):
        kg.update_meta_exploration("test-topic", 7.5, 0.5, False)
        assert self.monitor.get_last_quality("test-topic") == 7.5

    def test_is_topic_blocked_false(self):
        assert self.monitor.is_topic_blocked("active-topic") is False

    def test_is_topic_blocked_true(self):
        kg.mark_topic_done("blocked-topic", "max_count_reached")
        assert self.monitor.is_topic_blocked("blocked-topic") is True


class TestAssessExplorationQuality:
    def setup_method(self):
        self.monitor = MetaCognitiveMonitor()
        state = kg.get_state()
        state["meta_cognitive"] = {
            "explore_counts": {},
            "marginal_returns": {},
            "last_quality": {},
            "exploration_log": [],
            "completed_topics": {}
        }
        state["knowledge"]["topics"] = {}
        kg._save_state(state)

    def test_assess_quality_new_discovery(self):
        findings = {
            "summary": "Machine learning artificial intelligence deep learning neural networks",
            "sources": ["http://a.com"],
            "papers": []
        }
        quality = self.monitor.assess_exploration_quality("test", findings)
        assert 0 <= quality <= 10

    def test_assess_quality_depth_improvement(self):
        # Add existing knowledge with depth
        kg.add_knowledge("test", depth=3, summary="Basic info")
        findings = {
            "summary": "A" * 1500,  # Long summary
            "sources": ["http://a.com", "http://b.com", "http://c.com", "http://d.com", "http://e.com"],
            "papers": [{}, {}, {}]
        }
        quality = self.monitor.assess_exploration_quality("test", findings)
        assert quality > 0

    def test_assess_quality_exception_fallback(self):
        # Pass invalid findings to trigger exception handling
        findings = None
        quality = self.monitor.assess_exploration_quality("test", findings or {})
        assert quality == 5.0  # Default fallback


class TestComputeMarginalReturn:
    def setup_method(self):
        self.monitor = MetaCognitiveMonitor()
        state = kg.get_state()
        state["meta_cognitive"] = {
            "explore_counts": {},
            "marginal_returns": {},
            "last_quality": {},
            "exploration_log": [],
            "completed_topics": {}
        }
        kg._save_state(state)

    def test_marginal_return_first_exploration(self):
        # No previous returns
        marginal = self.monitor.compute_marginal_return("new-topic", 8.0)
        assert marginal == 1.0

    def test_marginal_return_improvement(self):
        # Previous quality was lower
        kg.update_meta_exploration("test", 5.0, 0.0, False)
        marginal = self.monitor.compute_marginal_return("test", 8.0)
        assert marginal > 0

    def test_marginal_return_decline(self):
        # Previous quality was higher
        kg.update_meta_exploration("test", 8.0, 0.0, False)
        marginal = self.monitor.compute_marginal_return("test", 5.0)
        assert marginal < 0


class TestExtractKeywords:
    def test_extract_keywords_fallback(self):
        monitor = MetaCognitiveMonitor(llm_client=None)
        keywords = monitor._extract_keywords("Machine learning and artificial intelligence are important fields")
        assert isinstance(keywords, list)
        assert len(keywords) > 0
        assert "machine" in keywords or "learning" in keywords

    def test_extract_keywords_empty_text(self):
        monitor = MetaCognitiveMonitor(llm_client=None)
        keywords = monitor._extract_keywords("")
        assert keywords == []


class TestDepthScore:
    def test_depth_score_long_summary(self):
        monitor = MetaCognitiveMonitor()
        findings = {"summary": "A" * 2000, "sources": [], "papers": []}
        score = monitor._assess_depth_score(findings)
        assert score > 5  # Should be high for long summary

    def test_depth_score_many_sources(self):
        monitor = MetaCognitiveMonitor()
        findings = {"summary": "Short", "sources": ["a", "b", "c", "d", "e", "f"], "papers": []}
        score = monitor._assess_depth_score(findings)
        assert score > 0

    def test_depth_score_balanced(self):
        monitor = MetaCognitiveMonitor()
        findings = {
            "summary": "A" * 1000,
            "sources": ["a", "b", "c"],
            "papers": [{}, {}]
        }
        score = monitor._assess_depth_score(findings)
        assert 0 <= score <= 10


class TestRecordExploration:
    def setup_method(self):
        self.monitor = MetaCognitiveMonitor()
        state = kg.get_state()
        state["meta_cognitive"] = {
            "explore_counts": {},
            "marginal_returns": {},
            "last_quality": {},
            "exploration_log": [],
            "completed_topics": {}
        }
        kg._save_state(state)

    def test_record_exploration(self):
        self.monitor.record_exploration("test-topic", 7.5, 0.5, True)
        
        # Verify state was updated
        assert kg.get_topic_explore_count("test-topic") == 1
        assert kg.get_last_quality("test-topic") == 7.5
        returns = kg.get_topic_marginal_returns("test-topic")
        assert returns == [0.5]
```

---

## Step 3: MetaCognitiveController (100%测试覆盖)

### Task 3.1: 实现 MetaCognitiveController

**文件:** `core/meta_cognitive_controller.py`

```python
"""
Meta-cognitive controller - Pure decision module
Makes decisions based on Monitor data: whether to explore, continue, notify
"""
from core.meta_cognitive_monitor import MetaCognitiveMonitor


class MetaCognitiveController:
    """Meta-cognitive controller - decision logic"""

    DEFAULT_THRESHOLDS = {
        "max_explore_count": 3,
        "min_marginal_return": 0.3,
        "high_quality_threshold": 7.0
    }

    def __init__(self, monitor: MetaCognitiveMonitor, config: dict = None):
        self.monitor = monitor
        self.thresholds = self.DEFAULT_THRESHOLDS.copy()

        if config:
            self.thresholds.update(config.get("thresholds", {}))
        else:
            try:
                from core.config import get_config
                cfg = get_config()
                self.thresholds.update({
                    "max_explore_count": getattr(cfg.thresholds, 'max_explore_count', 3),
                    "min_marginal_return": getattr(cfg.thresholds, 'min_marginal_return', 0.3),
                    "high_quality_threshold": getattr(cfg.thresholds, 'high_quality_threshold', 7.0)
                })
            except:
                pass

    def should_explore(self, topic: str) -> tuple[bool, str]:
        """Decide whether to explore topic"""
        if self.monitor.is_topic_blocked(topic):
            return False, f"Topic '{topic}' is blocked (completed)"

        explore_count = self.monitor.get_explore_count(topic)
        max_count = self.thresholds["max_explore_count"]

        if explore_count >= max_count:
            return False, f"Max explore count ({max_count}) reached for '{topic}'"

        return True, f"Explore allowed ({explore_count}/{max_count})"

    def should_continue(self, topic: str) -> tuple[bool, str]:
        """Decide whether to continue exploring topic (next round)"""
        returns = self.monitor.get_marginal_returns(topic)
        min_return = self.thresholds["min_marginal_return"]

        if not returns:
            return True, "First exploration, continue to gather data"

        last_return = returns[-1]

        if last_return < min_return:
            return False, f"Marginal return ({last_return:.2f}) below threshold ({min_return})"

        if len(returns) >= 2:
            if returns[-1] < min_return and returns[-2] < min_return:
                return False, "Marginal return declining for 2 consecutive explorations"

        return True, f"Marginal return healthy ({last_return:.2f})"

    def should_notify(self, topic: str) -> tuple[bool, str]:
        """Decide whether to notify user"""
        quality = self.monitor.get_last_quality(topic)
        threshold = self.thresholds["high_quality_threshold"]

        if quality >= threshold:
            return True, f"High quality discovery ({quality:.1f} >= {threshold})"

        return False, f"Quality ({quality:.1f}) below notification threshold ({threshold})"

    def get_decision_summary(self, topic: str) -> dict:
        """Get complete decision summary for topic"""
        explore_allowed, explore_reason = self.should_explore(topic)
        continue_allowed, continue_reason = self.should_continue(topic)
        notify, notify_reason = self.should_notify(topic)

        return {
            "topic": topic,
            "should_explore": explore_allowed,
            "explore_reason": explore_reason,
            "should_continue": continue_allowed,
            "continue_reason": continue_reason,
            "should_notify": notify,
            "notify_reason": notify_reason,
            "explore_count": self.monitor.get_explore_count(topic),
            "last_quality": self.monitor.get_last_quality(topic),
            "marginal_returns": self.monitor.get_marginal_returns(topic)
        }
```

### Task 3.2: 创建测试套件 (100%覆盖)

**文件:** `tests/test_meta_cognitive_controller.py`

**测试场景清单:**

```python
# 1. 初始化测试
test_init_default_thresholds      # 默认阈值
test_init_custom_thresholds       # 自定义阈值
test_init_from_config             # 从配置加载

# 2. should_explore测试
test_should_explore_new_topic     # 新话题允许
test_should_explore_blocked       # 阻止话题拒绝
test_should_explore_max_count     # 达到最大次数拒绝
test_should_explore_under_limit   # 次数内允许

# 3. should_continue测试
test_should_continue_first        # 首次继续
test_should_continue_high_return  # 高收益继续
test_should_continue_low_return   # 低收益停止
test_should_continue_declining    # 连续下降停止

# 4. should_notify测试
test_should_notify_high_quality   # 高质量通知
test_should_notify_low_quality    # 低质量不通知
test_should_notify_at_threshold   # 阈值边界

# 5. get_decision_summary测试
test_decision_summary_complete    # 完整摘要
```

---

## Step 4: MGV循环集成 (集成测试)

### Task 4.1: 改造 curious_agent.py

**修改:** `curious_agent.py` 中的 `run_one_cycle` 函数

**集成点:**
1. 探索前检查: `controller.should_explore()`
2. 质量评估: `monitor.assess_exploration_quality()`
3. 边际收益: `monitor.compute_marginal_return()`
4. 通知决策: `controller.should_notify()`
5. 继续决策: `controller.should_continue()`
6. 事件触发: `EventBus.emit()`

### Task 4.2: 创建集成测试

**文件:** `tests/test_mgv_integration.py`

**测试场景:**

```python
# 1. 阻止探索测试
test_mgv_blocks_over_explored_topic       # 阻止过度探索
test_mgv_blocks_completed_topic           # 阻止已完成话题

# 2. 质量评估流程测试
test_mgv_assesses_quality                 # 评估质量流程
test_mgv_records_exploration              # 记录探索数据

# 3. 通知决策测试
test_mgv_notifies_high_quality            # 高质量通知
test_mgv_skips_low_quality_notification   # 低质量跳过

# 4. 继续探索决策测试
test_mgv_continues_high_marginal          # 高收益继续
test_mgv_stops_low_marginal               # 低收益停止

# 5. 事件触发测试
test_mgv_emits_blocked_event              # 阻止事件
test_mgv_emits_discovery_event            # 发现事件
```

---

## Step 5: EventBus + Config + API (100%测试覆盖)

### Task 5.1: EventBus

**文件:** `core/event_bus.py`

```python
class EventBus:
    _subscribers: dict[str, list[Callable]] = {}

    @classmethod
    def subscribe(cls, event_type: str, handler: Callable):
        if event_type not in cls._subscribers:
            cls._subscribers[event_type] = []
        cls._subscribers[event_type].append(handler)

    @classmethod
    def emit(cls, event_type: str, payload: dict):
        for handler in cls._subscribers.get(event_type, []):
            try:
                handler(payload)
            except Exception as e:
                print(f"[EventBus] Handler error: {e}")

    @classmethod
    def clear(cls, event_type: str = None):
        if event_type:
            cls._subscribers.pop(event_type, None)
        else:
            cls._subscribers.clear()
```

**测试:** `tests/test_event_bus.py`
- test_subscribe_and_emit
- test_multiple_handlers
- test_unsubscribe
- test_handler_exception
- test_clear

### Task 5.2: Config模块

**文件:** `core/config.py`

**功能:**
- 加载 config.json
- 加载 .env
- 解析阈值配置
- 解析 LLM providers

**测试:** `tests/test_config.py`
- test_load_default_config
- test_load_custom_config
- test_load_env_file
- test_parse_thresholds
- test_parse_llm_providers

### Task 5.3: API端点

**修改:** `curious_api.py`

**新增端点:**
- GET `/api/metacognitive/state`
- GET `/api/metacognitive/check?topic=xxx`
- GET `/api/metacognitive/history/<topic>`
- GET `/api/metacognitive/topics/completed`

**测试:** `tests/test_api_metacognitive.py`
- test_get_metacognitive_state
- test_check_topic_status
- test_get_topic_history
- test_get_completed_topics

---

## Step 6: Web UI元认知区域

### Task 6.1: 修改 UI

**文件:** `ui/index.html`

**新增区域:**
1. 元认知状态卡片 (统计面板)
2. 已完成话题列表
3. 探索历史可视化

### Task 6.2: E2E测试

**测试清单:**
- 页面加载显示元认知区域
- 统计数据正确显示
- 已完成话题列表更新
- API数据正确渲染

---

## 验收测试清单

### 功能验收

| 测试项 | 命令 | 预期结果 |
|--------|------|----------|
| 模块可导入 | `python3 -c "from core.x import X"` | 无错误 |
| 探索阻止 | 同一话题探索4次 | 第4次被阻止 |
| 质量评分 | 检查返回质量分 | 0-10范围 |
| 通知触发 | 高质量探索 | 触发事件 |
| API端点 | curl 测试 | 返回正确JSON |
| Web UI | 浏览器检查 | 显示元认知区域 |

### 测试覆盖率验收

| 模块 | 覆盖率要求 | 验证命令 |
|------|-----------|---------|
| llm_manager.py | 100% | pytest --cov=core.llm_manager --cov-report=term-missing |
| meta_cognitive_monitor.py | 100% | pytest --cov=core.meta_cognitive_monitor |
| meta_cognitive_controller.py | 100% | pytest --cov=core.meta_cognitive_controller |
| event_bus.py | 100% | pytest --cov=core.event_bus |
| config.py | 100% | pytest --cov=core.config |
| knowledge_graph.py | >80% | pytest --cov=core.knowledge_graph |

---

## 执行顺序

```
Step 2补全 → Step 3 → Step 4 → Step 5 → Step 6 → 验收
   ↓           ↓        ↓        ↓        ↓       ↓
 测试文件    实现+测试  集成测试  实现+测试  E2E    全链路
```

每个步骤完成后立即提交，确保可回滚。

---

_计划创建时间: 2026-03-21_
_基于: v0.2.2 实施计划_
_测试要求: 100%单元测试覆盖_
