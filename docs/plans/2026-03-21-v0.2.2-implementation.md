# Curious Agent v0.2.2 完整实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现元认知监控器，解决无限循环和价值缺失问题，包含多LLM路由、质量评分、MGV循环和完整的API/UI支持

**Architecture:** 采用 Monitor-Generate-Verify 架构，分离监测(MetaCognitiveMonitor)和决策(MetaCognitiveController)，通过EventBus解耦通知机制

**Tech Stack:** Python 3.11+, Flask, Vanilla JS + D3.js, 多Provider LLM路由

---

## 前置准备

### 前置 1: 检查当前项目状态

**Files:**
- Read: `core/llm_client.py`
- Read: `core/knowledge_graph.py`
- Read: `curious_agent.py`
- Read: `curious_api.py`

**Step 1: 确认现有代码结构**

```bash
git status
ls -la core/
```

**Step 2: 备份当前 state.json**

```bash
cp knowledge/state.json knowledge/state.json.backup
```

**Step 3: 运行现有测试确保基线**

```bash
python3 -m pytest tests/ -v --tb=short
```

Expected: 所有测试通过

---

## Step 0: LLMManager 多LLM路由架构

### Task 0.1: 创建 core/llm_manager.py

**Files:**
- Create: `core/llm_manager.py`
- Test: `tests/test_llm_manager.py`

**Step 1: 创建测试文件**

```python
# tests/test_llm_manager.py
import pytest
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

from core.llm_manager import ModelEntry, LLMProvider, LLMManager


class TestModelEntry:
    def test_model_entry_defaults(self):
        entry = ModelEntry(model="test-model")
        assert entry.model == "test-model"
        assert entry.weight == 1
        assert entry.capabilities == []
        assert entry.max_tokens == 2000
        assert entry.temperature == 0.7
    
    def test_model_entry_custom(self):
        entry = ModelEntry(
            model="gpt-4",
            weight=3,
            capabilities=["fast", "keywords"],
            max_tokens=4000,
            temperature=0.5
        )
        assert entry.weight == 3
        assert entry.capabilities == ["fast", "keywords"]


class TestLLMProvider:
    def test_provider_get_model_by_capability(self):
        models = [
            ModelEntry(model="fast-model", capabilities=["fast", "keywords"]),
            ModelEntry(model="slow-model", capabilities=["reasoning"]),
        ]
        provider = LLMProvider(
            name="test",
            api_url="http://test.com",
            api_key="test-key",
            models=models
        )
        
        # Should find by capability
        model = provider.get_model("keywords")
        assert model.model == "fast-model"
        
        # Should fallback to first
        model = provider.get_model("unknown")
        assert model.model == "fast-model"


class TestLLMManager:
    def test_singleton_pattern(self):
        LLMManager.reset_instance()
        m1 = LLMManager.get_instance()
        m2 = LLMManager.get_instance()
        assert m1 is m2
    
    def test_list_capabilities_empty(self):
        LLMManager.reset_instance()
        manager = LLMManager.get_instance({})
        caps = manager.list_capabilities()
        assert caps == {}
    
    def test_select_with_single_provider(self):
        LLMManager.reset_instance()
        config = {
            "providers": {
                "test": {
                    "api_url": "http://test.com",
                    "models": [{"model": "test-model", "weight": 1}]
                }
            }
        }
        manager = LLMManager.get_instance(config)
        provider, model = manager.select("general")
        assert provider.name == "test"
        assert model.model == "test-model"
```

**Step 2: 运行测试验证失败**

```bash
python3 -m pytest tests/test_llm_manager.py -v
```

Expected: ImportError - module not found

**Step 3: 实现 core/llm_manager.py**

```python
# core/llm_manager.py
"""
多Provider + 多Model的LLM管理器
支持两层路由：Provider层 + Model层
"""
import os
import random
from dataclasses import dataclass, field
from typing import Optional


@dataclass
class ModelEntry:
    """单个模型的配置条目"""
    model: str
    weight: int = 1
    capabilities: list = field(default_factory=list)
    max_tokens: int = 2000
    temperature: float = 0.7


@dataclass
class LLMProvider:
    """LLM Provider配置"""
    name: str
    api_url: str
    api_key: str
    models: list[ModelEntry] = field(default_factory=list)
    timeout: int = 60
    default_model: str = ""
    enabled: bool = True

    def get_model(self, task_type: str = "general") -> ModelEntry:
        """根据task_type在该provider内选择合适的模型"""
        for m in self.models:
            if task_type in m.capabilities:
                return m
        return self.models[0] if self.models else ModelEntry(model=self.default_model)


class LLMManager:
    """
    多Provider + 多Model的LLM管理器
    
    核心能力：
    1. 多Provider支持（volcengine/openai/anthropic/...）
    2. 同Provider多Model支持（按task_type选模型）
    3. 两层路由：Provider层 + Model层
    4. 并发请求支持
    """
    
    _instance: Optional["LLMManager"] = None
    
    def __init__(self, config: dict = None):
        config = config or {}
        self.providers: list[LLMProvider] = []
        self._init_providers(config.get("providers", {}))
        self.strategy = config.get("selection_strategy", "capability")
    
    @classmethod
    def get_instance(cls, config: dict = None) -> "LLMManager":
        if cls._instance is None:
            cls._instance = cls(config)
        return cls._instance
    
    @classmethod
    def reset_instance(cls):
        """重置单例（测试用）"""
        cls._instance = None
    
    def _init_providers(self, providers_config: dict):
        """初始化所有provider及其下的models"""
        for provider_name, cfg in providers_config.items():
            api_key = os.environ.get(f"{provider_name.upper()}_API_KEY") or cfg.get("api_key")
            if not api_key:
                print(f"[LLMManager] Skipping {provider_name}: no API key")
                continue
            
            models = []
            raw_models = cfg.get("models", [])
            
            # 兼容旧格式
            if "model" in cfg and not raw_models:
                raw_models = [{"model": cfg["model"], "weight": cfg.get("weight", 1),
                               "capabilities": cfg.get("capabilities", ["general"])}]
            
            for m_cfg in raw_models:
                models.append(ModelEntry(
                    model=m_cfg.get("model", ""),
                    weight=m_cfg.get("weight", 1),
                    capabilities=m_cfg.get("capabilities", ["general"]),
                    max_tokens=m_cfg.get("max_tokens", 2000),
                    temperature=m_cfg.get("temperature", 0.7),
                ))
            
            provider = LLMProvider(
                name=provider_name,
                api_url=cfg.get("api_url", ""),
                api_key=api_key,
                models=models,
                timeout=cfg.get("timeout", 60),
                default_model=models[0].model if models else "",
                enabled=cfg.get("enabled", True),
            )
            self.providers.append(provider)
        
        if not self.providers:
            print("[LLMManager] Warning: No LLM providers configured")
    
    def select(self, task_type: str = "general") -> tuple[LLMProvider, ModelEntry]:
        """根据策略选择(provider, model)"""
        if len(self.providers) == 1 and len(self.providers[0].models) == 1:
            p = self.providers[0]
            return p, p.models[0]
        
        if self.strategy == "capability":
            return self._capability_based(task_type)
        else:
            return self._weighted_rr(task_type)
    
    def _capability_based(self, task_type: str) -> tuple[LLMProvider, ModelEntry]:
        """基于能力选择"""
        for p in self.providers:
            if not p.enabled:
                continue
            model = p.get_model(task_type)
            if model and task_type in model.capabilities:
                return p, model
        return self._weighted_rr(task_type)
    
    def _weighted_rr(self, task_type: str = "general") -> tuple[LLMProvider, ModelEntry]:
        """加权轮询"""
        candidates = []
        for p in self.providers:
            if not p.enabled:
                continue
            for m in p.models:
                weight = self._get_provider_weight(p.name) * m.weight
                candidates.append((p, m, weight))
        
        if not candidates:
            raise ValueError("No available LLM providers")
        
        total_weight = sum(c[2] for c in candidates)
        r = random.randint(1, total_weight)
        cumsum = 0
        for p, m, w in candidates:
            cumsum += w
            if r <= cumsum:
                return p, m
        return candidates[-1][:2]
    
    def _get_provider_weight(self, provider_name: str) -> int:
        """获取provider的权重"""
        for p in self.providers:
            if p.name == provider_name:
                return sum(m.weight for m in p.models) if p.models else 1
        return 1
    
    def chat(self, prompt: str, task_type: str = "general",
             model_override: str = None, provider_override: str = None,
             **kwargs) -> str:
        """向选中的provider+model发送请求"""
        if provider_override:
            provider = next((p for p in self.providers if p.name == provider_override), None)
            if not provider:
                raise ValueError(f"Provider {provider_override} not found")
            if model_override:
                model = next((m for m in provider.models if m.model == model_override), None)
                if not model:
                    raise ValueError(f"Model {model_override} not found in {provider_override}")
            else:
                model = provider.get_model(task_type)
        else:
            provider, model = self.select(task_type)
            if model_override:
                model = next((m for m in provider.models if m.model == model_override), model)
        
        temperature = kwargs.pop("temperature", model.temperature)
        max_tokens = kwargs.pop("max_tokens", model.max_tokens)
        
        import requests
        headers = {
            "Authorization": f"Bearer {provider.api_key}",
            "Content-Type": "application/json"
        }
        
        payload = {
            "model": model.model,
            "messages": [{"role": "user", "content": prompt}],
            "temperature": temperature,
            "max_tokens": max_tokens,
            **kwargs
        }
        
        response = requests.post(
            provider.api_url,
            headers=headers,
            json=payload,
            timeout=provider.timeout
        )
        response.raise_for_status()
        return response.json()["choices"][0]["message"]["content"]
    
    def chat_batch(self, prompts: list, task_type: str = "general",
                   max_workers: int = 3, **kwargs) -> list:
        """并发发送多个LLM请求"""
        import concurrent.futures
        
        results = [None] * len(prompts)
        assignments = self._assign_to_providers(len(prompts), task_type)
        
        def send_request(idx: int, provider_name: str, model_name: str, prompt: str):
            return idx, self.chat(prompt, provider_override=provider_name,
                                  model_override=model_name, **kwargs)
        
        with concurrent.futures.ThreadPoolExecutor(max_workers=max_workers) as executor:
            futures = []
            for idx, (p_name, m_name) in enumerate(assignments):
                futures.append(executor.submit(send_request, idx, p_name, m_name, prompts[idx]))
            
            for future in concurrent.futures.as_completed(futures):
                idx, result = future.result()
                results[idx] = result
        
        return results
    
    def _assign_to_providers(self, count: int, task_type: str) -> list:
        """将N个请求分配到不同的(provider, model)"""
        assignments = []
        available = []
        
        for p in self.providers:
            if not p.enabled:
                continue
            for m in p.models:
                available.append((p.name, m.model, self._get_provider_weight(p.name) * m.weight))
        
        if not available:
            raise ValueError("No available LLM models")
        
        expanded = []
        for p_name, m_name, w in available:
            expanded.extend([(p_name, m_name)] * w)
        
        random.shuffle(expanded)
        
        for i in range(count):
            assignments.append(expanded[i % len(expanded)])
        
        return assignments
    
    def list_capabilities(self) -> dict:
        """列出所有provider和model的能力"""
        result = {}
        for p in self.providers:
            result[p.name] = {
                "api_url": p.api_url,
                "models": {}
            }
            for m in p.models:
                result[p.name]["models"][m.model] = {
                    "capabilities": m.capabilities,
                    "weight": m.weight
                }
        return result
```

**Step 4: 运行测试验证通过**

```bash
python3 -m pytest tests/test_llm_manager.py -v
```

Expected: All tests PASS

**Step 5: Commit**

```bash
git add core/llm_manager.py tests/test_llm_manager.py
git commit -m "feat(step0): add LLMManager with multi-provider and multi-model routing"
```

---

### Task 0.2: 改造 LLMClient 以兼容 LLMManager

**Files:**
- Modify: `core/llm_client.py`

**Step 1: 查看现有 LLMClient**

```bash
head -50 core/llm_client.py
```

**Step 2: 修改 LLMClient 委托给 LLMManager**

```python
# core/llm_client.py
"""
LLM Client - 兼容接口，实际委托给 LLMManager
"""
import os
from typing import Optional


class LLMClient:
    """兼容现有代码的LLMClient，改为委托给LLMManager"""
    
    def __init__(self, provider_name: str = None, model_name: str = None):
        from core.llm_manager import LLMManager
        self.manager = LLMManager.get_instance()
        self.provider_override = provider_name
        self.model_override = model_name
    
    def chat(self, prompt: str, **kwargs) -> str:
        """发送chat请求"""
        return self.manager.chat(
            prompt,
            task_type="general",
            provider_override=self.provider_override,
            model_override=self.model_override,
            **kwargs
        )
    
    def generate_insights(self, topic: str, papers: list, **kwargs):
        """论文洞察生成（Layer 3专用）"""
        prompt = self._build_insight_prompt(topic, papers)
        return self.manager.chat(
            prompt,
            task_type="insights",
            provider_override=self.provider_override,
            model_override=self.model_override,
            **kwargs
        )
    
    def _build_insight_prompt(self, topic: str, papers: list) -> str:
        """构建洞察prompt"""
        parts = [f"研究主题: {topic}\n"]
        for i, paper in enumerate(papers, 1):
            parts.append(f"论文{i}: {paper.get('title', 'N/A')}\n")
            parts.append(f"摘要: {paper.get('abstract', '')[:300]}...\n")
            parts.append(f"关键发现: {', '.join(paper.get('key_findings', [])[:3])}\n\n")
        parts.append("请提供深度洞察报告，包括：\n")
        parts.append("1. 主要研究趋势\n")
        parts.append("2. 各论文之间的关系\n")
        parts.append("3. 对实际应用的建议\n")
        return "".join(parts)
    
    def evaluate_intrinsic_signals(self, topic: str, findings: dict) -> dict:
        """评估内在信号（ICM专用）"""
        prompt = f"""评估以下探索结果的内在信号（0-10分）：

话题: {topic}
发现: {findings.get('summary', '')[:500]}

请用JSON格式返回：
{{
  "pred_error": <预测误差评分>,
  "graph_density": <图谱密度评分>,
  "novelty": <新颖性评分>
}}

评分标准：
- pred_error: 理解误差越大分数越高（值得探索）
- graph_density: 连接越少分数越高（孤立知识）
- novelty: 与已知知识重叠越少分数越高"""
        
        import json
        try:
            response = self.manager.chat(
                prompt,
                task_type="icm_signals",
                provider_override=self.provider_override,
                model_override=self.model_override
            )
            # 提取JSON
            start = response.find('{')
            end = response.rfind('}')
            if start >= 0 and end > start:
                return json.loads(response[start:end+1])
        except Exception as e:
            print(f"[LLMClient] Error evaluating signals: {e}")
        
        # Fallback
        return {"pred_error": 5.0, "graph_density": 5.0, "novelty": 5.0}
```

**Step 3: 运行现有测试确保兼容**

```bash
python3 -m pytest tests/test_llm_client.py -v
```

Expected: 测试可能需要更新以使用新的配置方式

**Step 4: Commit**

```bash
git add core/llm_client.py
git commit -m "refactor(step0): adapt LLMClient to delegate to LLMManager"
```

---

### Task 0.3: 创建 config.json 多Provider配置

**Files:**
- Create: `config.json`

**Step 1: 创建配置文件**

```json
{
  "meta_cognitive": {
    "max_explore_count": 3,
    "min_marginal_return": 0.3,
    "high_quality_threshold": 7.0
  },
  "user_interests": [
    "agent framework",
    "self-reflection",
    "metacognition"
  ],
  "notification": {
    "enabled": true,
    "min_quality": 7.0
  },
  "llm": {
    "default_provider": "volcengine",
    "selection_strategy": "capability",
    "providers": {
      "volcengine": {
        "api_url": "https://ark.cn-beijing.volces.com/api/v3/chat/completions",
        "timeout": 60,
        "enabled": true,
        "models": [
          {
            "model": "ark-code-latest",
            "weight": 3,
            "capabilities": ["fast", "general", "keywords", "quality"],
            "max_tokens": 2000,
            "temperature": 0.7
          },
          {
            "model": "deepseek-chat",
            "weight": 2,
            "capabilities": ["reasoning", "analysis", "icm_signals"],
            "max_tokens": 4000,
            "temperature": 0.5
          }
        ]
      }
    }
  }
}
```

**Step 2: 添加到 .gitignore 保护API key**

```bash
echo "config.json" >> .gitignore
echo ".env" >> .gitignore
```

**Step 3: 创建 config.json.example**

```bash
cp config.json config.json.example
```

**Step 4: Commit**

```bash
git add config.json.example .gitignore
git commit -m "config(step0): add multi-provider LLM configuration template"
```

---

## Step 1: KnowledgeGraph 扩展

### Task 1.1: 添加 _ensure_meta_cognitive 和 mark_topic_done

**Files:**
- Modify: `core/knowledge_graph.py`
- Test: `tests/test_knowledge_graph.py`

**Step 1: 查看现有 knowledge_graph.py**

```bash
grep -n "def add_knowledge\|def add_curiosity" core/knowledge_graph.py
```

**Step 2: 添加新方法**

在 `core/knowledge_graph.py` 中添加以下函数：

```python
def _ensure_meta_cognitive(state: dict) -> dict:
    """确保state.json包含meta_cognitive字段"""
    if "meta_cognitive" not in state:
        state["meta_cognitive"] = {
            "explore_counts": {},
            "marginal_returns": {},
            "last_quality": {},
            "exploration_log": []
        }
    return state


def mark_topic_done(topic: str, reason: str) -> None:
    """标记话题为已完成，阻止后续探索"""
    state = _load_state()
    state = _ensure_meta_cognitive(state)
    
    mc = state["meta_cognitive"]
    if "completed_topics" not in mc:
        mc["completed_topics"] = {}
    
    mc["completed_topics"][topic] = {
        "reason": reason,
        "timestamp": datetime.now(timezone.utc).isoformat()
    }
    _save_state(state)


def update_last_exploration_notified(topic: str, notified: bool) -> None:
    """更新最近一次探索的notified标记"""
    state = _load_state()
    state = _ensure_meta_cognitive(state)
    
    mc = state["meta_cognitive"]
    if "last_notified" not in mc:
        mc["last_notified"] = {}
    
    mc["last_notified"][topic] = {
        "notified": notified,
        "timestamp": datetime.now(timezone.utc).isoformat()
    }
    _save_state(state)


def get_topic_keywords(topic: str) -> list:
    """获取话题已有关键词"""
    state = _load_state()
    topics = state.get("knowledge", {}).get("topics", {})
    if topic in topics:
        return topics[topic].get("keywords", [])
    return []


def get_topic_depth(topic: str) -> float:
    """获取话题当前深度"""
    state = _load_state()
    topics = state.get("knowledge", {}).get("topics", {})
    if topic in topics:
        return topics[topic].get("depth", 0)
    return 0.0


def is_topic_completed(topic: str) -> bool:
    """检查话题是否已完成（被标记为不再探索）"""
    state = _load_state()
    state = _ensure_meta_cognitive(state)
    mc = state.get("meta_cognitive", {})
    completed = mc.get("completed_topics", {})
    return topic in completed


def update_meta_exploration(topic: str, quality: float, marginal_return: float, notified: bool) -> None:
    """更新探索的元认知数据"""
    state = _load_state()
    state = _ensure_meta_cognitive(state)
    
    mc = state["meta_cognitive"]
    
    # 更新探索计数
    if "explore_counts" not in mc:
        mc["explore_counts"] = {}
    mc["explore_counts"][topic] = mc["explore_counts"].get(topic, 0) + 1
    
    # 更新边际收益历史
    if "marginal_returns" not in mc:
        mc["marginal_returns"] = {}
    if topic not in mc["marginal_returns"]:
        mc["marginal_returns"][topic] = []
    mc["marginal_returns"][topic].append(marginal_return)
    
    # 更新最后质量
    if "last_quality" not in mc:
        mc["last_quality"] = {}
    mc["last_quality"][topic] = quality
    
    # 添加到探索日志
    if "exploration_log" not in mc:
        mc["exploration_log"] = []
    mc["exploration_log"].append({
        "topic": topic,
        "quality": quality,
        "marginal_return": marginal_return,
        "notified": notified,
        "timestamp": datetime.now(timezone.utc).isoformat()
    })
    
    _save_state(state)


def get_meta_cognitive_state() -> dict:
    """获取完整的元认知状态"""
    state = _load_state()
    state = _ensure_meta_cognitive(state)
    return state.get("meta_cognitive", {})


def get_topic_explore_count(topic: str) -> int:
    """获取话题的探索次数"""
    state = _load_state()
    state = _ensure_meta_cognitive(state)
    mc = state.get("meta_cognitive", {})
    return mc.get("explore_counts", {}).get(topic, 0)


def get_topic_marginal_returns(topic: str) -> list:
    """获取话题的边际收益历史"""
    state = _load_state()
    state = _ensure_meta_cognitive(state)
    mc = state.get("meta_cognitive", {})
    return mc.get("marginal_returns", {}).get(topic, [])
```

**Step 3: 添加测试**

在 `tests/test_knowledge_graph.py` 中添加：

```python
class TestMetaCognitive:
    def test_ensure_meta_cognitive(self):
        state = {"knowledge": {"topics": {}}}
        result = kg._ensure_meta_cognitive(state)
        assert "meta_cognitive" in result
        assert "explore_counts" in result["meta_cognitive"]
    
    def test_mark_topic_done(self):
        kg.mark_topic_done("test-topic", "max_explore_reached")
        state = kg.get_state()
        mc = state.get("meta_cognitive", {})
        assert "test-topic" in mc.get("completed_topics", {})
    
    def test_update_exploration(self):
        kg.update_meta_exploration("test-topic", 7.5, 0.5, True)
        count = kg.get_topic_explore_count("test-topic")
        assert count >= 1
        returns = kg.get_topic_marginal_returns("test-topic")
        assert len(returns) >= 1
```

**Step 4: 运行测试**

```bash
python3 -m pytest tests/test_knowledge_graph.py -v
```

**Step 5: Commit**

```bash
git add core/knowledge_graph.py tests/test_knowledge_graph.py
git commit -m "feat(step1): extend KnowledgeGraph with meta-cognitive tracking"
```

---

## Step 2: MetaCognitiveMonitor 纯监测模块

### Task 2.1: 创建 MetaCognitiveMonitor 核心类

**Files:**
- Create: `core/meta_cognitive_monitor.py`
- Test: `tests/test_meta_cognitive_monitor.py`

**Step 1: 创建测试**

```python
# tests/test_meta_cognitive_monitor.py
import pytest
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

from core.meta_cognitive_monitor import MetaCognitiveMonitor
from core import knowledge_graph as kg


class TestMetaCognitiveMonitor:
    def setup_method(self):
        """每个测试前重置状态"""
        # 重置meta_cognitive状态
        state = kg.get_state()
        state["meta_cognitive"] = {
            "explore_counts": {},
            "marginal_returns": {},
            "last_quality": {},
            "exploration_log": []
        }
        kg._save_state(state)
    
    def test_get_explore_count_new_topic(self):
        monitor = MetaCognitiveMonitor()
        count = monitor.get_explore_count("new-topic")
        assert count == 0
    
    def test_assess_exploration_quality_basic(self):
        monitor = MetaCognitiveMonitor()
        findings = {
            "summary": "This is a test summary about artificial intelligence and machine learning.",
            "sources": ["http://example.com/1", "http://example.com/2"],
            "papers": [{"title": "Test Paper"}]
        }
        quality = monitor.assess_exploration_quality("test-topic", findings)
        assert 0 <= quality <= 10
    
    def test_extract_keywords_fallback(self):
        monitor = MetaCognitiveMonitor(llm_client=None)
        keywords = monitor._extract_keywords("Machine learning and artificial intelligence are important.")
        assert isinstance(keywords, list)
        assert len(keywords) > 0
    
    def test_compute_marginal_return(self):
        monitor = MetaCognitiveMonitor()
        # 先记录一次探索
        kg.update_meta_exploration("test-topic", 8.0, 0.0, False)
        # 计算第二次的边际收益
        marginal = monitor.compute_marginal_return("test-topic", 7.0)
        assert marginal <= 0  # 质量下降，边际收益为负
```

**Step 2: 运行测试（应该失败）**

```bash
python3 -m pytest tests/test_meta_cognitive_monitor.py -v
```

**Step 3: 实现 MetaCognitiveMonitor**

```python
# core/meta_cognitive_monitor.py
"""
元认知监控器 - 纯监测模块
负责评估探索质量、计算边际收益、记录探索历史
"""
import re
import json
from typing import Optional
from core import knowledge_graph as kg


class MetaCognitiveMonitor:
    """元认知监控器 - 只读查询和质量评估"""
    
    def __init__(self, llm_client=None):
        self.kg = kg
        self.llm = llm_client
    
    # === 只读查询 ===
    
    def get_explore_count(self, topic: str) -> int:
        """获取话题的探索次数"""
        return kg.get_topic_explore_count(topic)
    
    def get_marginal_returns(self, topic: str) -> list[float]:
        """获取话题的边际收益历史"""
        return kg.get_topic_marginal_returns(topic)
    
    def get_last_quality(self, topic: str) -> float:
        """获取话题最后一次探索的质量"""
        state = kg.get_state()
        mc = state.get("meta_cognitive", {})
        return mc.get("last_quality", {}).get(topic, 0.0)
    
    def is_topic_blocked(self, topic: str) -> bool:
        """检查话题是否被阻止探索"""
        return kg.is_topic_completed(topic)
    
    # === 评估（核心：质量评分算法）===
    
    def assess_exploration_quality(self, topic: str, findings: dict) -> float:
        """
        三维质量评分（0-10）
        - new_discovery_rate × 0.35
        - depth_improvement × 0.35
        - user_relevance × 0.30
        """
        try:
            # Signal 1: 新关键词率 (0-1) × 权重 0.35
            current_keywords = self._extract_keywords(findings.get("summary", ""))
            known_keywords = set(kg.get_topic_keywords(topic))
            new_keywords = [k for k in current_keywords if k not in known_keywords]
            new_discovery_rate = len(new_keywords) / max(len(current_keywords), 1)
            
            # Signal 2: 理解深度提升 (0-1) × 权重 0.35
            prev_depth = kg.get_topic_depth(topic)
            depth_score = self._assess_depth_score(findings)
            depth_improvement = min(1.0, depth_score / max(prev_depth + 1, 1))
            
            # Signal 3: 用户相关性 (0-1) × 权重 0.30
            user_relevance = self._compute_user_relevance(topic)
            
            quality = (new_discovery_rate * 0.35 + 
                      depth_improvement * 0.35 + 
                      user_relevance * 0.30) * 10
            
            return round(quality, 1)
        except Exception as e:
            print(f"[MetaCognitiveMonitor] Error assessing quality: {e}")
            return 5.0  # 默认中等质量
    
    def compute_marginal_return(self, topic: str, current_quality: float) -> float:
        """计算边际收益（当前质量 vs 历史质量的变化）"""
        returns = self.get_marginal_returns(topic)
        
        if not returns:
            # 第一次探索，边际收益最大
            return 1.0
        
        # 使用最近3次的平均质量来计算趋势
        recent_returns = returns[-3:] if len(returns) >= 3 else returns
        avg_previous = sum(recent_returns) / len(recent_returns)
        
        # 边际收益 = 当前质量相对于历史平均的变化
        marginal = (current_quality / 10.0) - avg_previous
        
        return round(marginal, 2)
    
    # === 写入 ===
    
    def record_exploration(self, topic: str, quality: float,
                          marginal_return: float, notified: bool) -> None:
        """记录探索结果到state.json"""
        kg.update_meta_exploration(topic, quality, marginal_return, notified)
    
    # === 私有辅助方法 ===
    
    def _extract_keywords(self, text: str) -> list:
        """从文本提取关键词（5-10个），优先LLM，fail-fast到规则"""
        if self.llm and text:
            prompt = f"""从以下文本提取5-10个核心概念关键词（用逗号分隔）：

{text[:500]}

只返回关键词，不要其他内容。"""
            try:
                response = self.llm.chat(prompt)
                keywords = [k.strip().lower() for k in response.split(",") if k.strip()]
                if len(keywords) >= 3:
                    return keywords[:10]
            except Exception:
                pass  # Fail fast to fallback
        
        # 降级：规则提取
        words = re.findall(r'\b[a-z]{4,}\b', text.lower())
        stopwords = {
            'that', 'this', 'with', 'from', 'have', 'been', 'will', 'would', 
            'could', 'their', 'they', 'them', 'than', 'only', 'also', 'when',
            'where', 'what', 'which', 'while', 'during', 'before', 'after'
        }
        filtered = [w for w in words if w not in stopwords]
        
        # 按频率排序，取前10
        from collections import Counter
        word_counts = Counter(filtered)
        return [word for word, _ in word_counts.most_common(10)]
    
    def _assess_depth_score(self, findings: dict) -> float:
        """评估探索深度得分（0-10）"""
        summary = findings.get("summary", "")
        sources = findings.get("sources", [])
        papers = findings.get("papers", [])
        
        summary_score = min(1.0, len(summary) / 1000)
        source_score = min(1.0, len(sources) / 5)
        paper_score = min(1.0, len(papers) / 3)
        
        return (summary_score * 0.4 + source_score * 0.3 + paper_score * 0.3) * 10
    
    def _compute_user_relevance(self, topic: str) -> float:
        """计算与用户兴趣的相关性（0-1）"""
        try:
            from core.config import get_config
            user_interests = get_config().user_interests
        except:
            user_interests = []
        
        if not user_interests:
            return 0.5  # 默认中等相关
        
        if self.llm:
            prompt = f"""评估话题与用户兴趣的相关性（0.0-1.0）：

用户兴趣：{', '.join(user_interests)}
待评估话题：{topic}

只返回一个0.0到1.0之间的数字，不要其他内容。"""
            try:
                response = self.llm.chat(prompt)
                # 提取数字
                import re
                numbers = re.findall(r'\d+\.?\d*', response)
                if numbers:
                    score = float(numbers[0])
                    return max(0.0, min(1.0, score))
            except Exception:
                pass
        
        # 降级：关键词重叠率
        topic_words = set(topic.lower().split())
        interest_words = set(' '.join(user_interests).lower().split())
        if not topic_words:
            return 0.5
        overlap = len(topic_words & interest_words) / len(topic_words)
        return min(1.0, overlap)
    
    def _fallback_quality(self, topic: str, findings: dict) -> float:
        """当主评估失败时的降级评估"""
        summary_len = len(findings.get("summary", ""))
        sources_count = len(findings.get("sources", []))
        
        # 简单统计评分
        score = min(10, summary_len / 200 + sources_count * 2)
        return max(0, min(10, score))
```

**Step 4: 运行测试**

```bash
python3 -m pytest tests/test_meta_cognitive_monitor.py -v
```

**Step 5: Commit**

```bash
git add core/meta_cognitive_monitor.py tests/test_meta_cognitive_monitor.py
git commit -m "feat(step2): implement MetaCognitiveMonitor with quality scoring"
```

---

## Step 3: MetaCognitiveController 纯决策模块

### Task 3.1: 创建 MetaCognitiveController

**Files:**
- Create: `core/meta_cognitive_controller.py`
- Test: `tests/test_meta_cognitive_controller.py`

**Step 1: 创建测试**

```python
# tests/test_meta_cognitive_controller.py
import pytest
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

from unittest.mock import Mock, MagicMock
from core.meta_cognitive_controller import MetaCognitiveController


class TestMetaCognitiveController:
    def setup_method(self):
        """设置mock monitor"""
        self.mock_monitor = Mock()
        self.config = {
            "thresholds": {
                "max_explore_count": 3,
                "min_marginal_return": 0.3,
                "high_quality_threshold": 7.0
            }
        }
        self.controller = MetaCognitiveController(self.mock_monitor, self.config)
    
    def test_should_explore_new_topic(self):
        """新话题应该允许探索"""
        self.mock_monitor.get_explore_count.return_value = 0
        self.mock_monitor.is_topic_blocked.return_value = False
        
        allowed, reason = self.controller.should_explore("new-topic")
        assert allowed is True
    
    def test_should_explore_max_count_reached(self):
        """超过最大探索次数应该阻止"""
        self.mock_monitor.get_explore_count.return_value = 4
        self.mock_monitor.is_topic_blocked.return_value = False
        
        allowed, reason = self.controller.should_explore("test-topic")
        assert allowed is False
        assert "max_explore_count" in reason.lower()
    
    def test_should_explore_blocked_topic(self):
        """被标记为完成的话题应该阻止"""
        self.mock_monitor.get_explore_count.return_value = 1
        self.mock_monitor.is_topic_blocked.return_value = True
        
        allowed, reason = self.controller.should_explore("blocked-topic")
        assert allowed is False
        assert "blocked" in reason.lower()
    
    def test_should_continue_high_marginal_return(self):
        """高边际收益应该继续"""
        self.mock_monitor.get_marginal_returns.return_value = [0.8]
        
        allowed, reason = self.controller.should_continue("test-topic")
        assert allowed is True
    
    def test_should_continue_low_marginal_return(self):
        """低边际收益应该停止"""
        self.mock_monitor.get_marginal_returns.return_value = [0.1, 0.05]
        
        allowed, reason = self.controller.should_continue("test-topic")
        assert allowed is False
        assert "marginal return" in reason.lower()
    
    def test_should_notify_high_quality(self):
        """高质量应该通知"""
        self.mock_monitor.get_last_quality.return_value = 8.0
        
        should, reason = self.controller.should_notify("test-topic")
        assert should is True
    
    def test_should_notify_low_quality(self):
        """低质量不应该通知"""
        self.mock_monitor.get_last_quality.return_value = 5.0
        
        should, reason = self.controller.should_notify("test-topic")
        assert should is False
```

**Step 2: 实现 MetaCognitiveController**

```python
# core/meta_cognitive_controller.py
"""
元认知控制器 - 纯决策模块
基于Monitor的数据做出决策：是否探索、是否继续、是否通知
"""
from core.meta_cognitive_monitor import MetaCognitiveMonitor


class MetaCognitiveController:
    """元认知控制器 - 决策逻辑"""
    
    DEFAULT_THRESHOLDS = {
        "max_explore_count": 3,
        "min_marginal_return": 0.3,
        "high_quality_threshold": 7.0
    }
    
    def __init__(self, monitor: MetaCognitiveMonitor, config: dict = None):
        self.monitor = monitor
        self.thresholds = self.DEFAULT_THRESHOLDS.copy()
        
        if config:
            self.thresholds.update(config.get("thresholds", {}))
        else:
            # 尝试从全局配置加载
            try:
                from core.config import get_config
                cfg = get_config()
                self.thresholds.update({
                    "max_explore_count": getattr(cfg.thresholds, 'max_explore_count', 3),
                    "min_marginal_return": getattr(cfg.thresholds, 'min_marginal_return', 0.3),
                    "high_quality_threshold": getattr(cfg.thresholds, 'high_quality_threshold', 7.0)
                })
            except:
                pass  # 使用默认值
    
    def should_explore(self, topic: str) -> tuple[bool, str]:
        """
        决定是否应该探索该话题
        
        Returns:
            (allowed: bool, reason: str)
        """
        # 检查1：话题是否被标记为完成
        if self.monitor.is_topic_blocked(topic):
            return False, f"Topic '{topic}' is blocked (completed)"
        
        # 检查2：探索次数是否超过上限
        explore_count = self.monitor.get_explore_count(topic)
        max_count = self.thresholds["max_explore_count"]
        
        if explore_count >= max_count:
            return False, f"Max explore count ({max_count}) reached for '{topic}'"
        
        # 允许探索
        return True, f"Explore allowed ({explore_count}/{max_count})"
    
    def should_continue(self, topic: str) -> tuple[bool, str]:
        """
        决定是否应该继续探索该话题（下一轮）
        
        Returns:
            (allowed: bool, reason: str)
        """
        returns = self.monitor.get_marginal_returns(topic)
        min_return = self.thresholds["min_marginal_return"]
        
        if not returns:
            # 第一次探索后默认继续
            return True, "First exploration, continue to gather data"
        
        # 使用最近一次的边际收益
        last_return = returns[-1]
        
        if last_return < min_return:
            return False, f"Marginal return ({last_return:.2f}) below threshold ({min_return})"
        
        # 检查趋势：如果连续两次边际收益都很低，停止
        if len(returns) >= 2:
            if returns[-1] < min_return and returns[-2] < min_return:
                return False, "Marginal return declining for 2 consecutive explorations"
        
        return True, f"Marginal return healthy ({last_return:.2f})"
    
    def should_notify(self, topic: str) -> tuple[bool, str]:
        """
        决定是否应该通知用户
        
        Returns:
            (should_notify: bool, reason: str)
        """
        quality = self.monitor.get_last_quality(topic)
        threshold = self.thresholds["high_quality_threshold"]
        
        if quality >= threshold:
            return True, f"High quality discovery ({quality:.1f} >= {threshold})"
        
        return False, f"Quality ({quality:.1f}) below notification threshold ({threshold})"
    
    def get_decision_summary(self, topic: str) -> dict:
        """获取话题的完整决策摘要"""
        explore_allowed, explore_reason = self.should_explore(topic)
        continue_allowed, continue_reason = self.should_continue(topic)
        notify, notify_reason = self.should_notify(topic)
        
        return {
            "topic": topic,
            "should_explore": explore_allowed,
            "explore_reason": explore_reason,
            "should_continue": continue_allowed,
            "continue_reason": continue_reason,
            "should_notify": notify,
            "notify_reason": notify_reason,
            "explore_count": self.monitor.get_explore_count(topic),
            "last_quality": self.monitor.get_last_quality(topic),
            "marginal_returns": self.monitor.get_marginal_returns(topic)
        }
```

**Step 3: 运行测试**

```bash
python3 -m pytest tests/test_meta_cognitive_controller.py -v
```

**Step 4: Commit**

```bash
git add core/meta_cognitive_controller.py tests/test_meta_cognitive_controller.py
git commit -m "feat(step3): implement MetaCognitiveController with decision logic"
```

---

## Step 4: 集成到 CuriousAgent（MGV循环）

### Task 4.1: 改造 curious_agent.py 添加MGV循环

**Files:**
- Modify: `curious_agent.py`
- Test: 更新现有测试

**Step 1: 修改 run_one_cycle 函数**

找到 `run_one_cycle` 函数并完全替换：

```python
def run_one_cycle(depth: str = "medium") -> dict:
    """执行一轮MGV（Monitor-Generate-Verify）循环"""
    if depth not in VALID_DEPTHS:
        raise ValueError(f"Invalid depth '{depth}'. Must be one of: {', '.join(sorted(VALID_DEPTHS))}")
    
    # 导入元认知模块
    from core.meta_cognitive_monitor import MetaCognitiveMonitor
    from core.meta_cognitive_controller import MetaCognitiveController
    from core.event_bus import EventBus
    from core.llm_manager import LLMManager
    
    # 初始化
    engine = CuriosityEngine()
    
    # 确保初始好奇心
    engine.generate_initial_curiosities()
    engine.rescore_all()
    
    # 选择下一个话题
    next_curiosity = engine.select_next()
    if not next_curiosity:
        return {"status": "idle", "message": "没有待探索的好奇心"}
    
    topic = next_curiosity["topic"]
    
    # 初始化Monitor和Controller
    llm_manager = LLMManager.get_instance()
    monitor = MetaCognitiveMonitor(llm_client=llm_manager)
    controller = MetaCognitiveController(monitor)
    
    # === Monitor: 探索前检查 ===
    allowed, reason = controller.should_explore(topic)
    if not allowed:
        logger.info(f"探索被阻止: {topic} — {reason}")
        kg.mark_topic_done(topic, reason)
        EventBus.emit("exploration.blocked", {"topic": topic, "reason": reason})
        return {"status": "blocked", "topic": topic, "reason": reason}
    
    # === Generate: 执行探索 ===
    explorer = Explorer(exploration_depth=depth)
    result = explorer.explore(next_curiosity)
    
    findings = {
        "summary": result.get("findings", ""),
        "sources": result.get("sources", []),
        "papers": result.get("papers", [])
    }
    
    # === Monitor: 评估质量 ===
    quality = monitor.assess_exploration_quality(topic, findings)
    marginal = monitor.compute_marginal_return(topic, quality)
    
    # 暂时记录为未通知（后面可能会更新）
    monitor.record_exploration(topic, quality, marginal, notified=False)
    
    # === Verify: 决策 ===
    should_notify, notify_reason = controller.should_notify(topic)
    notified = False
    
    if should_notify:
        formatted = _format_discovery(topic, result, quality)
        
        # 触发事件
        EventBus.emit("discovery.high_quality", {
            "topic": topic,
            "quality": quality,
            "formatted": formatted,
            "timestamp": datetime.now(timezone.utc).isoformat()
        })
        
        EventBus.emit("notification.external", {
            "topic": topic,
            "quality": quality,
            "message": formatted
        })
        
        # 更新通知状态
        kg.update_last_exploration_notified(topic, True)
        monitor.record_exploration(topic, quality, marginal, notified=True)
        notified = True
    
    # 决定是否继续探索该话题
    continue_allowed, continue_reason = controller.should_continue(topic)
    
    if continue_allowed:
        # 重新注入队列（降低优先级）
        kg.add_curiosity(
            topic, 
            reason=f"边际收益:{marginal:.2f}, 质量:{quality:.1f}",
            relevance=quality / 10.0,
            depth=quality / 10.0 * 5
        )
    else:
        kg.mark_topic_done(topic, continue_reason)
        EventBus.emit("exploration.completed", {
            "topic": topic,
            "reason": continue_reason,
            "final_quality": quality
        })
    
    # 自动队列新话题（仅medium/deep）
    auto_queued = 0
    if depth in ("medium", "deep"):
        keywords = engine._extract_keywords(findings["summary"])
        if keywords:
            auto_queued = engine.auto_queue_topics(keywords, parent_topic=topic)
    
    return {
        "status": "success",
        "topic": topic,
        "result": result,
        "formatted": explorer.format_for_user(result),
        "quality": quality,
        "marginal_return": marginal,
        "notified": notified,
        "continue": continue_allowed,
        "auto_queued": auto_queued
    }


def _format_discovery(topic: str, result: dict, quality: float) -> str:
    """格式化发现结果用于通知"""
    lines = [
        f"🔍 探索发现: {topic}",
        f"📊 质量评分: {quality:.1f}/10",
        ""
    ]
    
    findings = result.get("findings", "")
    if findings:
        # 截断摘要
        summary = findings[:300] + "..." if len(findings) > 300 else findings
        lines.append(f"📝 摘要: {summary}")
    
    sources = result.get("sources", [])
    if sources:
        lines.append(f"\n📚 来源: {len(sources)} 个")
    
    papers = result.get("papers", [])
    if papers:
        lines.append(f"📄 论文: {len(papers)} 篇")
    
    return "\n".join(lines)
```

**Step 2: 添加导入和日志**

在文件顶部添加：

```python
import logging

logger = logging.getLogger(__name__)
```

**Step 3: 更新状态显示函数**

修改 `print_status` 函数以显示元认知状态：

```python
def print_status():
    """打印当前状态（包含元认知信息）"""
    state = kg.get_state()
    summary = kg.get_knowledge_summary()
    
    print("=" * 50)
    print("👁️  Curious Agent v0.2.2 - 状态面板")
    print("=" * 50)
    print(f"🕒 最后更新: {state.get('last_update', '未知')}")
    print(f"📚 知识节点: {summary['total_topics']} 个")
    print(f"   - 已知: {summary['known_count']}")
    print(f"   - 未知: {summary['total_topics'] - summary['known_count']}")
    print(f"❓ 待探索好奇心: {summary['pending_curiosities']} 项")
    print(f"🗺️  探索历史: {summary['recent_explorations']} 条")
    
    # 显示元认知状态
    mc = state.get("meta_cognitive", {})
    if mc:
        completed = len(mc.get("completed_topics", {}))
        total_explored = sum(mc.get("explore_counts", {}).values())
        print(f"🧠 元认知状态:")
        print(f"   - 已完成话题: {completed} 个")
        print(f"   - 总探索次数: {total_explored} 次")
    
    print()
    
    # ... 其余代码保持不变
```

**Step 4: 运行测试**

```bash
python3 -m pytest tests/test_cli.py -v
```

**Step 5: Commit**

```bash
git add curious_agent.py
git commit -m "feat(step4): integrate MGV loop into CuriousAgent with meta-cognitive control"
```

---

## Step 5: EventBus + Config + API端点

### Task 5.1: 创建 EventBus 事件总线

**Files:**
- Create: `core/event_bus.py`
- Test: `tests/test_event_bus.py`

**Step 1: 创建 EventBus**

```python
# core/event_bus.py
"""
事件总线 - 解耦的通知机制
支持订阅/发布模式，用于元认知事件通知
"""
from typing import Callable


class EventBus:
    """简单的事件总线实现"""
    
    _subscribers: dict[str, list[Callable]] = {}
    
    @classmethod
    def subscribe(cls, event_type: str, handler: Callable):
        """订阅事件"""
        if event_type not in cls._subscribers:
            cls._subscribers[event_type] = []
        cls._subscribers[event_type].append(handler)
    
    @classmethod
    def emit(cls, event_type: str, payload: dict):
        """触发事件"""
        handlers = cls._subscribers.get(event_type, [])
        for handler in handlers:
            try:
                handler(payload)
            except Exception as e:
                print(f"[EventBus] Handler error for {event_type}: {e}")
    
    @classmethod
    def unsubscribe(cls, event_type: str, handler: Callable):
        """取消订阅"""
        if event_type in cls._subscribers:
            if handler in cls._subscribers[event_type]:
                cls._subscribers[event_type].remove(handler)
    
    @classmethod
    def clear(cls, event_type: str = None):
        """清除订阅（测试用）"""
        if event_type:
            cls._subscribers.pop(event_type, None)
        else:
            cls._subscribers.clear()
    
    @classmethod
    def list_subscribers(cls) -> dict:
        """列出所有订阅者（调试用）"""
        return {
            event_type: len(handlers)
            for event_type, handlers in cls._subscribers.items()
        }
```

**Step 2: 创建测试**

```python
# tests/test_event_bus.py
import pytest
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

from core.event_bus import EventBus


class TestEventBus:
    def setup_method(self):
        """每个测试前清除订阅"""
        EventBus.clear()
    
    def test_subscribe_and_emit(self):
        """测试订阅和触发"""
        received = []
        
        def handler(payload):
            received.append(payload)
        
        EventBus.subscribe("test.event", handler)
        EventBus.emit("test.event", {"data": "hello"})
        
        assert len(received) == 1
        assert received[0]["data"] == "hello"
    
    def test_multiple_handlers(self):
        """测试多个handler"""
        count = [0]
        
        def handler1(_):
            count[0] += 1
        
        def handler2(_):
            count[0] += 1
        
        EventBus.subscribe("test.event", handler1)
        EventBus.subscribe("test.event", handler2)
        EventBus.emit("test.event", {})
        
        assert count[0] == 2
    
    def test_unsubscribe(self):
        """测试取消订阅"""
        received = []
        
        def handler(payload):
            received.append(payload)
        
        EventBus.subscribe("test.event", handler)
        EventBus.unsubscribe("test.event", handler)
        EventBus.emit("test.event", {"data": "hello"})
        
        assert len(received) == 0
```

**Step 3: Commit**

```bash
git add core/event_bus.py tests/test_event_bus.py
git commit -m "feat(step5): add EventBus for decoupled event notifications"
```

---

### Task 5.2: 创建 Config 配置管理模块

**Files:**
- Create: `core/config.py`
- Test: `tests/test_config.py`

**Step 1: 创建 Config 模块**

```python
# core/config.py
"""
配置管理 - 加载和提供配置数据
支持 config.json + .env 分层配置
"""
import json
import os
from dataclasses import dataclass, field
from pathlib import Path
from typing import Optional


@dataclass
class MetaCognitiveThresholds:
    max_explore_count: int = 3
    min_marginal_return: float = 0.3
    high_quality_threshold: float = 7.0


@dataclass
class ModelEntry:
    model: str
    weight: int = 1
    capabilities: list = field(default_factory=list)
    max_tokens: int = 2000
    temperature: float = 0.7


@dataclass
class LLMProvider:
    name: str
    api_url: str
    models: list = field(default_factory=list)
    api_key: Optional[str] = None
    timeout: int = 60
    enabled: bool = True

    def get_model(self, task_type: str = "general") -> ModelEntry:
        for m in self.models:
            if task_type in m.capabilities:
                return m
        return self.models[0] if self.models else ModelEntry(model="")


@dataclass
class Config:
    thresholds: MetaCognitiveThresholds
    user_interests: list = field(default_factory=list)
    notification: dict = field(default_factory=dict)
    llm_providers: list = field(default_factory=list)
    default_llm_provider: str = "volcengine"


def load_config() -> Config:
    """加载配置文件"""
    config_path = Path(__file__).parent.parent / "config.json"
    raw = {}
    
    if config_path.exists():
        try:
            with open(config_path, encoding="utf-8") as f:
                raw = json.load(f)
        except (json.JSONDecodeError, IOError) as e:
            print(f"[Config] Error loading config.json: {e}")
    
    # 加载.env文件
    _load_env_file()
    
    # 解析阈值
    mc = raw.get("meta_cognitive", {})
    thresholds = MetaCognitiveThresholds(
        max_explore_count=mc.get("max_explore_count", 3),
        min_marginal_return=mc.get("min_marginal_return", 0.3),
        high_quality_threshold=mc.get("high_quality_threshold", 7.0)
    )
    
    # 解析LLM providers
    llm_providers = []
    llm_config = raw.get("llm", {})
    
    for name, cfg in llm_config.get("providers", {}).items():
        api_key = os.environ.get(f"{name.upper()}_API_KEY") or cfg.get("api_key")
        
        # 解析models列表
        models = []
        for m_cfg in cfg.get("models", []):
            models.append(ModelEntry(
                model=m_cfg.get("model", ""),
                weight=m_cfg.get("weight", 1),
                capabilities=m_cfg.get("capabilities", ["general"]),
                max_tokens=m_cfg.get("max_tokens", 2000),
                temperature=m_cfg.get("temperature", 0.7)
            ))
        
        # 兼容旧格式
        if "model" in cfg and not models:
            models.append(ModelEntry(
                model=cfg["model"],
                weight=cfg.get("weight", 1),
                capabilities=cfg.get("capabilities", ["general"])
            ))
        
        llm_providers.append(LLMProvider(
            name=name,
            api_url=cfg.get("api_url", ""),
            models=models,
            api_key=api_key,
            timeout=cfg.get("timeout", 60),
            enabled=cfg.get("enabled", True)
        ))
    
    return Config(
        thresholds=thresholds,
        user_interests=raw.get("user_interests", []),
        notification=raw.get("notification", {}),
        llm_providers=llm_providers,
        default_llm_provider=llm_config.get("default_provider", "volcengine")
    )


def _load_env_file():
    """加载.env文件到环境变量"""
    env_file = Path(__file__).parent.parent / ".env"
    if env_file.exists():
        with open(env_file, encoding="utf-8") as f:
            for line in f:
                line = line.strip()
                if line and not line.startswith("#") and "=" in line:
                    key, value = line.split("=", 1)
                    os.environ.setdefault(key.strip(), value.strip())


# 全局配置实例
_config: Optional[Config] = None


def get_config() -> Config:
    """获取全局配置（单例）"""
    global _config
    if _config is None:
        _config = load_config()
    return _config


def reload_config() -> Config:
    """重新加载配置"""
    global _config
    _config = load_config()
    return _config
```

**Step 2: 创建测试**

```python
# tests/test_config.py
import pytest
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

from core import config as cfg_module


class TestConfig:
    def test_default_thresholds(self):
        """测试默认阈值"""
        thresholds = cfg_module.MetaCognitiveThresholds()
        assert thresholds.max_explore_count == 3
        assert thresholds.min_marginal_return == 0.3
        assert thresholds.high_quality_threshold == 7.0
    
    def test_model_entry_defaults(self):
        """测试ModelEntry默认值"""
        entry = cfg_module.ModelEntry(model="test")
        assert entry.weight == 1
        assert entry.capabilities == []
        assert entry.max_tokens == 2000
```

**Step 3: Commit**

```bash
git add core/config.py tests/test_config.py
git commit -m "feat(step5): add Config module for configuration management"
```

---

### Task 5.3: 添加元认知API端点

**Files:**
- Modify: `curious_api.py`

**Step 1: 在 curious_api.py 底部添加API端点**

```python
# === MetaCognitive API Endpoints ===

@app.route("/api/metacognitive/state")
def api_metacognitive_state():
    """获取完整元认知状态"""
    from core import knowledge_graph as kg
    
    state = kg.get_state()
    mc = kg.get_meta_cognitive_state()
    
    return jsonify({
        "status": "ok",
        "meta_cognitive": mc,
        "summary": {
            "completed_topics": len(mc.get("completed_topics", {})),
            "total_explorations": sum(mc.get("explore_counts", {}).values()),
            "topics_with_history": len(mc.get("explore_counts", {}))
        }
    })


@app.route("/api/metacognitive/check")
def api_metacognitive_check():
    """检查特定话题的状态"""
    from core import knowledge_graph as kg
    from core.meta_cognitive_monitor import MetaCognitiveMonitor
    from core.meta_cognitive_controller import MetaCognitiveController
    
    topic = request.args.get("topic", "").strip()
    if not topic:
        return jsonify({"error": "topic parameter required"}), 400
    
    monitor = MetaCognitiveMonitor()
    controller = MetaCognitiveController(monitor)
    
    summary = controller.get_decision_summary(topic)
    
    return jsonify({
        "status": "ok",
        "topic": topic,
        "decision": summary
    })


@app.route("/api/metacognitive/history/<topic>")
def api_metacognitive_history(topic):
    """获取话题的探索历史"""
    from core import knowledge_graph as kg
    
    state = kg.get_state()
    mc = state.get("meta_cognitive", {})
    
    # 获取该话题的日志
    logs = [
        log for log in mc.get("exploration_log", [])
        if log.get("topic") == topic
    ]
    
    return jsonify({
        "status": "ok",
        "topic": topic,
        "history": logs,
        "count": kg.get_topic_explore_count(topic),
        "completed": kg.is_topic_completed(topic)
    })


@app.route("/api/metacognitive/topics/completed")
def api_metacognitive_completed():
    """获取已完成的话题列表"""
    from core import knowledge_graph as kg
    
    mc = kg.get_meta_cognitive_state()
    completed = mc.get("completed_topics", {})
    
    return jsonify({
        "status": "ok",
        "completed_topics": [
            {"topic": topic, **data}
            for topic, data in completed.items()
        ]
    })
```

**Step 2: 运行API测试**

```bash
python3 -m pytest tests/test_api_complete.py -v
```

**Step 3: Commit**

```bash
git add curious_api.py
git commit -m "feat(step5): add metacognitive API endpoints"
```

---

## Step 6: Web UI 元认知区域

### Task 6.1: 更新 UI 添加元认知状态区域

**Files:**
- Modify: `ui/index.html`

**Step 1: 在合适位置添加元认知面板**

在 `ui/index.html` 中找到状态面板区域，添加元认知显示：

```html
<!-- 添加到状态面板区域 -->
<div class="card" style="margin-top: 20px;">
    <div class="card-header">
        <h3>🧠 元认知状态</h3>
    </div>
    <div class="card-body">
        <div id="meta-state">
            <p>加载中...</p>
        </div>
        <div id="meta-topics" style="margin-top: 15px;">
            <h4>已完成话题</h4>
            <ul id="completed-topics-list"></ul>
        </div>
    </div>
</div>
```

**Step 2: 添加 JavaScript 代码**

在 `<script>` 标签中添加：

```javascript
// 加载元认知状态
async function loadMetaState() {
    try {
        const response = await fetch('/api/metacognitive/state');
        const data = await response.json();
        
        if (data.status === 'ok') {
            const summary = data.summary;
            document.getElementById('meta-state').innerHTML = `
                <div class="stats-grid">
                    <div class="stat-item">
                        <span class="stat-value">${summary.completed_topics}</span>
                        <span class="stat-label">已完成话题</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-value">${summary.total_explorations}</span>
                        <span class="stat-label">总探索次数</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-value">${summary.topics_with_history}</span>
                        <span class="stat-label">有历史话题</span>
                    </div>
                </div>
            `;
        }
    } catch (error) {
        console.error('Error loading meta state:', error);
    }
}

// 加载已完成话题
async function loadCompletedTopics() {
    try {
        const response = await fetch('/api/metacognitive/topics/completed');
        const data = await response.json();
        
        if (data.status === 'ok') {
            const list = document.getElementById('completed-topics-list');
            if (data.completed_topics.length === 0) {
                list.innerHTML = '<li>暂无已完成话题</li>';
            } else {
                list.innerHTML = data.completed_topics.map(t => `
                    <li>
                        <strong>${t.topic}</strong>
                        <span class="badge">${t.reason}</span>
                    </li>
                `).join('');
            }
        }
    } catch (error) {
        console.error('Error loading completed topics:', error);
    }
}

// 在页面加载时调用
loadMetaState();
loadCompletedTopics();
```

**Step 3: 添加样式**

在 `<style>` 中添加：

```css
.stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 15px;
    margin-top: 10px;
}

.stat-item {
    text-align: center;
    padding: 10px;
    background: #f5f5f5;
    border-radius: 5px;
}

.stat-value {
    display: block;
    font-size: 24px;
    font-weight: bold;
    color: #333;
}

.stat-label {
    display: block;
    font-size: 12px;
    color: #666;
    margin-top: 5px;
}

.badge {
    display: inline-block;
    padding: 2px 8px;
    background: #e0e0e0;
    border-radius: 10px;
    font-size: 11px;
    color: #666;
    margin-left: 10px;
}
```

**Step 4: Commit**

```bash
git add ui/index.html
git commit -m "feat(step6): add metacognitive status panel to Web UI"
```

---

## 验收测试

### 最终验收测试清单

**Step 1: 运行所有测试**

```bash
python3 -m pytest tests/ -v --tb=short
```

Expected: 所有测试通过

**Step 2: 验证各模块可导入**

```bash
python3 -c "from core.llm_manager import LLMManager; print('✓ LLMManager')"
python3 -c "from core.meta_cognitive_monitor import MetaCognitiveMonitor; print('✓ MetaCognitiveMonitor')"
python3 -c "from core.meta_cognitive_controller import MetaCognitiveController; print('✓ MetaCognitiveController')"
python3 -c "from core.event_bus import EventBus; print('✓ EventBus')"
python3 -c "from core.config import get_config; print('✓ Config')"
```

**Step 3: 验证 API 端点**

```bash
# 启动服务
bash run_curious.sh &
sleep 5

# 测试元认知状态API
curl http://localhost:4848/api/metacognitive/state | jq

# 测试检查API
curl "http://localhost:4848/api/metacognitive/check?topic=test" | jq

# 停止服务
pkill -f curious_api.py
```

**Step 4: 验证探索循环阻止功能**

```bash
# 模拟多次探索同一话题
for i in {1..4}; do
    echo "=== Run $i ==="
    python3 curious_agent.py --run 2>&1 | head -20
done
```

Expected: 第4次探索应该被阻止

**Step 5: Commit 最终版本**

```bash
git add -A
git commit -m "feat(v0.2.2): complete metacognitive monitor implementation

- Add LLMManager with multi-provider and multi-model routing
- Extend KnowledgeGraph with meta-cognitive tracking
- Implement MetaCognitiveMonitor with quality scoring
- Implement MetaCognitiveController with decision logic
- Integrate MGV loop into CuriousAgent
- Add EventBus for decoupled notifications
- Add Config module for configuration management
- Add metacognitive API endpoints
- Add metacognitive status panel to Web UI

Fixes: infinite loop issue, missing value notifications"
```

---

## 附录

### 文件变更汇总

**新增文件：**
- `core/llm_manager.py` - 多LLM路由管理器
- `core/meta_cognitive_monitor.py` - 元认知监测器
- `core/meta_cognitive_controller.py` - 元认知控制器
- `core/event_bus.py` - 事件总线
- `core/config.py` - 配置管理
- `config.json.example` - 配置模板
- `tests/test_llm_manager.py` - LLMManager测试
- `tests/test_meta_cognitive_monitor.py` - Monitor测试
- `tests/test_meta_cognitive_controller.py` - Controller测试
- `tests/test_event_bus.py` - EventBus测试
- `tests/test_config.py` - Config测试

**修改文件：**
- `core/llm_client.py` - 委托给LLMManager
- `core/knowledge_graph.py` - 扩展元认知支持
- `curious_agent.py` - 集成MGV循环
- `curious_api.py` - 添加API端点
- `ui/index.html` - 添加元认知面板
- `.gitignore` - 保护配置文件

### 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      CuriousAgent v0.2.2                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Monitor    │  │   Generate   │  │    Verify    │      │
│  │              │  │              │  │              │      │
│  │ should_explore│─▶│  explorer    │─▶│ should_notify│      │
│  │              │  │              │  │              │      │
│  │ should_cont │  │              │  │ should_cont  │      │
│  └──────┬───────┘  └──────────────┘  └──────┬───────┘      │
│         │                                      │             │
│         ▼                                      ▼             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              MetaCognitiveMonitor                      │  │
│  │  - assess_exploration_quality()                       │  │
│  │  - compute_marginal_return()                          │  │
│  └──────────────────────────────────────────────────────┘  │
│         │                                      │             │
│         ▼                                      ▼             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │             MetaCognitiveController                    │  │
│  │  - should_explore() / should_continue()              │  │
│  │  - should_notify()                                    │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  EventBus    │  │  Knowledge   │  │  LLMManager  │      │
│  │  (events)    │  │  (state.json)│  │  (routing)   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

_实施计划创建时间: 2026-03-21_
_版本: v0.2.2 完整实现_
